# Qwen3.8 / GLM-5.3 on an EPYC 7F32 + 3× RX 7900 XTX box — placement measurements (2026-09-04)

Lab record, same spirit as the rest of this directory: what was measured, how,
and what it decided. Harness sources are next to this file
(`rome_cpubench.c`, `rome_vkbench.c`, `rome_fp8test.c`, one-file harnesses,
build lines in their headers); they hardcode
Qwen3.8 shapes and build against `quant.h` / `backend_vulkan.o`.

## The machine

| | |
|---|---|
| CPU | AMD EPYC 7F32, 8 cores / 16 threads, Zen 2 (AVX2+FMA+F16C, **no AVX-512**), 128 MB L3. SMT siblings are `i` and `i+8`. |
| RAM | 8 × 32 GB DDR4-2400. Measured read bandwidth 18.8 / 35.7 / 57.1 / 70.9 / 91.6 GB/s at 1 / 2 / 4 / 8 / 16 threads. |
| GPUs | 3 × Radeon RX 7900 XTX (gfx1100, RDNA3), 24 GB each, RADV (Mesa 26.0.8, Vulkan 1.4). Cooperative matrix and int8 dot product are exposed; **no FP8 in hardware**, `fmt=8` is emulated in the shader. |
| Disk | one NVMe. Both checkpoints (Qwen 173 GiB FP8, GLM 182 GiB int4) fit in the 247 GB page cache one at a time, not together. |

The one fact that reshapes every prior number in this repository: on this box
**the disk is not the wall** once a checkpoint is in the page cache. Every
"expert streaming is disk-bound" assumption in `docs/` is about 25–128 GB
machines.

## Baselines (before any change on this branch)

Prompt: 48 tokens, greedy, 64 new tokens, expert cap 512, page cache warm
(`fincore` checked), `COLI_TIMERS=1`.

| engine | decode | prefill / TTFT | per-token phases |
|---|---:|---:|---|
| Qwen3.8 CPU 16 thr, cap 64 | 1.16 tok/s | 18.3 s | routed 555 ms, resident BF16 158, deltanet 120, lm-head 13 |
| Qwen3.8 CPU 16 thr, cap 512 | 1.40–1.51 tok/s | 13.4–17.5 s | routed 383–395, resident 156–162, deltanet 120 |
| Qwen3.8 `qwen38-vk`, 3 GPUs, lazy tier | 1.57 tok/s (128 tok) | 18.8 s | routed 125, resident 196 (!), untimed uploads |
| GLM-5.3 `glm53` Vulkan, 3 GPUs (1296+1695+1695 preloaded from `.glm53_explain.bin`), **8 threads** | **3.17 tok/s** | 60 tok | reproduces the 3.16–3.26 tok/s recorded in the Sep 2 commits (f4fbc4c…573871f) |
| same, 16 threads bound | 2.78 tok/s | | the launcher pins engines to physical cores; 16 threads fight the RADV threads |
| same, 16 threads unbound | 2.02 tok/s | | |
| GLM-5.3 `glm53` CPU-only, 8 threads | 1.97 tok/s | | matches the 1.97–1.98 in the Sep 3 commits |

(For scale: the owner's llama.cpp HIP build of the same GLM checkpoint does
3.15 tok/s on this box with 18 layers offloaded.)

A first GLM measurement in this session read 1.41 tok/s and was wrong three
ways at once: 16 unbound threads, a page cache just evicted by warming Qwen
(167k major faults), and the 6,218-entry `.glm53_usage.bin` histogram that
left the third GPU empty. A bisect of the Sep 3 Vulkan commits at equal
conditions (573871f 2.02, 6da3910 2.07, both 16 threads) showed **no code
regression**: the shader's fmt=8 branch and the dev2/dev3 upload changes cost
GLM nothing measurable. Two operational lessons: GLM's dev2/dev3 preload
loops stop on a count cap only (`COLI_VK_EXPERTS2/3`), so an unlimited cap
spills the tier into host memory (6,458 "resident" experts = 91 GB, evicting
the page cache); and never run a foreign-branch `glm53` on this box
(`eabeb9a`'s cache sizing takes MemAvailable and OOM-thrashed the machine
for 25 minutes).

**Qwen** is where the GPU tier underdelivered: as found it was +8% over the
CPU fresh-process (1.57 vs 1.45) and +27% in steady state per the Sep 3
commits (5.0 vs 3.9, run 2 of 3 through the persistent server at 8 threads),
against a 3× per-expert advantage in the microbenchmarks. The reason is not
the GPU: see the per-op numbers.

## Where the Qwen CPU token went (perf, flat, 16 threads)

```
22.9% + 20.0%  libgomp barrier spin      <- 15 threads waiting for 1
22.5%          matmul_fp8 (routed experts)
18.1%          q38_matmul_bf16 (dense)
 3.6%          kernel spinlock (page-table lock under concurrent faults)
```

The waiting was page faults: an expert served from the file mapping is bound
by pointer (no copy) and first touched inside the 16-thread matmul; the thread
that faults (minor with a page-table lock, or major to NVMe after eviction)
stalls the other fifteen at the region barrier. Isolated, the same FP8 kernel
ran 4–6× faster than inside the engine (0.20 vs 0.8–1.1 ms per expert).

## Per-op placement matrix (Qwen3.8 shapes, this box)

CPU: real engine kernels, weights rotated through pools larger than L3.
GPU: real `backend_vulkan.o` through its public API, distinct-expert pools
larger than Infinity Cache, times include submit+fence round trip.

| op | shape | CPU (best) | GPU dev0 Vulkan | verdict |
|---|---|---:|---:|---|
| submit + fence round trip | tiny matmul | — | **53–66 µs** | the engine's comment says 0.4 ms; here it is 7× cheaper, so per-layer submits are affordable |
| BF16 dense GEMV | 2560→10240 | 0.52–0.57 ms, 92–101 GB/s (DRAM-bound) | no BF16 shader; int8 proxy 0.198 ms (132 GB/s eff.), fp8-emul 0.342 ms | CPU is at its bandwidth floor. GPU int8 is 2.9× faster per call but needs a numerics change or a new BF16 `fmt`; per token this is 8.5 GB → ~85 ms CPU floor vs ~50 ms GPU incl. ~48×5 submits |
| BF16 dense GEMV | 6144→2560 | 0.32 ms, 99 GB/s | int8 0.171 ms, int4 0.147, fp8 0.295 | as above |
| LM head | 2560→248320 | 12.3 ms, 104 GB/s | int8 1.58 ms (403 GB/s), int4 1.36 ms | GPU wins 8× **if** the head is int8/int4; kept on CPU for numerics |
| BF16 dense, prefill S=32 | 2560→10240 | engine kernel 9.0 ms (187 GFLOP/s); 4-accumulator variant 5.1 ms (330 GFLOP/s) | — | a 1.8× CPU prefill kernel win is available (not landed: prefill is dominated by expert reads) |
| int8 dense GEMV (AVX2 `maddubs`) | 2560→10240 | 0.27 ms, 99 GB/s | — | int8 dense on the CPU would halve the dense floor; changes numerics, not done |
| routed experts, decode (10 experts) | 2560→640 ×2, 640→2560, FP8 | engine kernel 2.0 ms/layer = **96 ms/token** (24.6 GB/s, ALU-bound); F16C decode **70 ms/token** (33.9 GB/s) | fp8-emul group 0.467 ms/layer = **22 ms/token**; int8 13.5 ms; int4 12.6 ms | GPU wins 3× per expert **when resident**; VRAM holds ~14k of 24.6k experts |
| routed experts, prefill rows=8 | same | — | fp8 1.66 ms/layer (0.166 ms/expert), int8 1.11 | FP8 emulation costs 1.5× vs int8 on the GPU: real, but not decisive |
| 3 GPUs, one expert group each | K=10 per device | — | sequential (engine as found) **2.37 ms/layer = 114 ms/token**; concurrent issue/take **0.63 ms = 30 ms/token** | the found engine serialised the three GPUs |
| 4 dense tensors, one submit vs four | I=2560 | — | 0.247 vs 0.378 ms | batching dense dispatches pays even at 55 µs/submit |

**The precision↔WMMA hypothesis** from the handoff: partly right, mostly
moot for decode. FP8 emulation makes the GPU expert GEMV 1.7× slower than
int8 at the same bytes (0.467 vs 0.282 ms per 10-expert group), but decode is
a GEMV, so cooperative-matrix hardware never enters; the emulated FP8 path is
still 3× faster per expert than the CPU. Converting Qwen experts to int8/int4
would buy ~9 ms/token on the GPU and halve the VRAM per expert (twice the
resident tier), at the cost of a second copy of the checkpoint and a
numerics change. Worth doing for VRAM, not for WMMA.

**Why the Qwen tier gained so little** (measured, not inferred): (1) the
CPU expert path was 4–6× slower than its own kernel because of page-fault
stalls, and the GPU path still ran that CPU path for every non-resident
expert; (2) the three GPUs were driven sequentially (114 ms/token at full
residency); (3) the tier filled lazily, so 4.7 MB uploads landed inside
decode, and the driver's spin-wait threads took cores away from the dense
matmuls (resident BF16 149 → 196 ms). None of that is RDNA3's fault.

## Changes landed (branch `perf/rome-cpu-path`), each A/B'd on this box

Same prompt, same 64 greedy tokens in every run (text identity), last-token
logits compared against the pristine binary where noted.

| change | Qwen CPU 16 thr | notes |
|---|---:|---|
| pristine | 1.45 tok/s (1.40 / 1.51) | |
| `MADV_POPULATE_READ` at expert bind (`Q38_MMAP_POPULATE`) | ≈ +10% | routed 395 → 299 ms with the other two changes off |
| F16C E4M3 decode in `matmul_fp8` | 0.203 → 0.145 ms/expert isolated | exhaustive 256-code check and matmul outputs bit-identical to the LUT path |
| DeltaNet / attention projections in one OpenMP region (`Q38_DENSE_MULTI`) | ≈ +2% | resident 132 → 125 ms; bit-identical rows |
| all three | **1.90–1.93 tok/s** (+32%) | routed 231 ms, resident 125–129 ms |
| + 8 threads, one per core (`OMP_NUM_THREADS=8 OMP_PLACES=cores`) | **2.35 tok/s** (+62%) | routed 142 ms; dense loses 19 ms; FP8 kernel is ALU-bound, SMT halves it |
| `OMP_WAIT_POLICY=passive` | 1.57 tok/s | worse: ~2,000 regions/token, wake-ups cost more than spinning |

| change | Qwen `qwen38-vk`, 3 GPUs | notes |
|---|---:|---|
| as found (lazy fill, sequential devices) | 1.57 tok/s | |
| + CPU changes, still lazy fill (`Q38_VK_PRELOAD=0`) | 1.40 tok/s | lazy uploads dominate |
| + heat-ranked preload + concurrent issue/take + CPU share overlapped | **2.77 tok/s** (64 tok), **2.99 tok/s** (128 tok) | 9,396 experts (every one with history) preloaded in 24 s, 24.4 GB on dev0; TTFT 8.8 s; routed 69–104 ms |

Two follow-ups measured and **rejected** (code removed, not shipped as knobs):

| tried | Qwen CPU | why it lost |
|---|---:|---|
| `num_threads(8)` on the FP8 kernel inside the 16-thread pool (threads 0–7 = 8 distinct cores, siblings are `i`/`i+8`) | 1.70 tok/s vs 1.90 | the eight idle pool threads spin on the sibling hyperthreads and slow the eight that work (resident BF16 125 → 166 ms too). Only a genuinely 8-thread pool helps. |
| two FMA chains per 128-block | 1.68 / 1.90 at 8 / 16 threads: no change | the kernel is not FMA-latency-bound; logits differed from the single chain by 8e-6 (argmax same) for nothing |

Numerical identity, last-token logits after the same 64 greedy tokens,
against the pristine binary: prefault + F16C + fused dense **max abs diff 0**
(bit-identical); `qwen38-vk` 3-GPU path max abs diff 7.6e-6, cosine 1.0,
same argmax (the GPU FP8 dot products accumulate in a different order).

Routing history matters for the tier: after the benchmark runs grew
`.coli_usage` from 9,396 to 11,244 nonzero experts, the same 3-GPU 128-token
run went from 2.99 to **3.38–3.39 tok/s** (TTFT 8.5 s).

Final numbers with the committed tree (same prompt, cap 512):

| configuration | decode | routed | resident BF16 | expert-read |
|---|---:|---:|---:|---:|
| `qwen38`, 16 threads | 2.00 tok/s (64 tok) | 221 ms | 127 ms | 41 ms |
| `qwen38`, `OMP_NUM_THREADS=8 OMP_PLACES=cores OMP_PROC_BIND=close` | 2.28 tok/s (64 tok) | 143 ms | 145 ms | 57 ms |
| `qwen38-vk`, 3 GPUs, 16 threads | 3.38 tok/s (128 tok) | 70 ms | 133 ms | 64 ms |
| `qwen38-vk`, 3 GPUs, **8 threads one per core** | **4.05 tok/s** (128 tok) | 60 ms | 114 ms | 27 ms |

The last row is the recommended launch on this box: 2.8× the pristine CPU
engine, 2.6× the pristine `qwen38-vk`. With the GPUs doing the experts, the
CPU's job is the dense BF16 stream, and eight threads leave the RADV
submit/fence threads their own hyperthreads instead of fighting sixteen
spinners for them (resident 133 → 114 ms). Both `qwen38` rows are
bit-identical to the pristine binary on last-token logits; the 3-GPU path
was checked at 64 tokens (max abs 7.6e-6, same argmax) — a 128-token dump is
a different position and is not comparable, which is why no 128-token
identity figure is quoted.

## Steady state: the regime the server runs in (`tools/datapoint.py`, 8 threads)

Persistent `SERVE=1` engine, page cache evicted before load, 80 completion
tokens, expert cap 512; one cold request, the same prompt twice (upper
bound), then two different prompts (the tool's primary serving result).
`datapoint.py` sets `OMP_NUM_THREADS` to physical cores, as the `coli`
launcher does, so these are 8-thread numbers, and the pristine rows match the
3.9 / 5.0 tok/s in the Sep 3 commit messages.

| engine | warm-identical (upper bound) | rotating prompts, median | cold first request | cold TTFT |
|---|---:|---:|---:|---:|
| pristine `qwen38` (CPU) | 4.11 tok/s | 2.87 | 2.54 | 19.5 s |
| patched `qwen38` (CPU) | **4.70** (+14%) | 2.89 (±0) | 2.47 | 11.9 s |
| pristine `qwen38-vk`, 3 GPUs | 5.34 | 2.72 | 2.08 | 18.3 s |
| patched `qwen38-vk`, 3 GPUs | 5.35 (±0) | **3.69** (+36%) | **3.59** (+73%) | 7.1 s |

Read against the fresh-process numbers above: the pristine engine was never
1.45 tok/s in service, only in a fresh process that pays every first-touch
fault. In steady state the branch buys +14% on the CPU path, nothing on the
GPU upper bound (the lazy tier had already filled and hits 100% on a repeated
prompt), +36% on changing prompts, and a much better first request (the
preload of 14,673 experts costs 90 s at load, then pays back on every cold
request: 7 s TTFT instead of 18). The rotating-prompt CPU number did not move:
prefaulting relocates the first-touch cost of new experts, it does not remove
it, which is the case for populating the whole mapping at load (backlog 3).

## What the data killed

- **"VRAM-reside Qwen's dense/attention/shared-expert weights" as the first
  move** (the prior audit's #1). It is real (~85 ms/token CPU floor vs ~50 ms
  on the GPU) but third in line behind the two 100+ ms items above, needs a
  BF16 shader format or an int8 conversion, and competes with experts for the
  same 72 GB. Now evidence-ranked, not killed.
- **"GPU loses to CPU because RDNA3 lacks FP8"**: emulation costs 1.7× on the
  GPU, the GPU still wins 3× per resident expert.
- **AVX-512 / VNNI ideas**: not on this silicon.
- **Bigger expert cap as a footgun fix** (`cap=1` default): on this box the
  mmap path makes a miss a pointer bind, so cap only changes hit telemetry and
  the batch loader's willingness to prefault; 512 is right here.

## Backlog, ranked by measured delta per token (Qwen, from the 2.99 tok/s state ≈ 334 ms)

1. **Dense set off DRAM** (resident BF16 137 ms + shared 19 ms): int8 on the
   GPU measured 132–400 GB/s effective vs 100 GB/s CPU; ~5 submits/layer ×
   48 × 55 µs = 13 ms. Expect −80 to −100 ms. Needs a BF16 `fmt` in
   `qmatmul.comp` (numerics-preserving) or an int8 dense conversion, and the
   DeltaNet/QSA cores stay on the CPU with per-layer round trips.
2. **Expert-read 71 ms (`MADV_POPULATE_READ` cost of first-touch CPU misses)**:
   goes to zero in steady state; for cold sessions, populate at load (whole
   mapping, 173 GiB fits) behind a knob.
3. **MTP speculative decoding** (tech report, Table 4: mean accepted length
   4.06 with four-step MTP reusing QSA indices). On a bandwidth-bound decode
   the dense 8.5 GB is read once per verify step, experts ~4×: ≈ 2.4× tokens
   per second at equal bytes. Largest multiplier available; largest effort
   (MTP module weights, index reuse, verify path).
4. **More routing history for the preload**: only 9,396 experts had nonzero
   counts; VRAM has room for ~14,000. Longer/rotating usage capture fills it.
5. **CPU prefill kernel** (4-accumulator BF16, 1.8× on S=32) and GPU prefill
   for resident experts: TTFT, not decode.
6. **GLM-5.3**: 3.17 tok/s on the tier at 8 threads equals llama.cpp HIP's
   3.15. CPU-side items are the scalar KDA inner loop and the O(context²)
   pooled-key recompute in `sparse_index.h`; the mmap expert path likely
   carries the same first-touch fault stall as Qwen's did (not measured).

## C0: `tools/rome_bench.sh` harness, gate result

One script (drop caches, warm one model, verify residency with `fincore`,
pin `OMP_NUM_THREADS=8 OMP_PLACES=cores OMP_PROC_BIND=close`, set the GLM
caps, run `datapoint.py --mode persistent`, extract the rotating median from
its Workload summary table). Gate: two consecutive runs of the same config
within 3%.

`qwen38-vk`, **3 GPUs, tier verified up** (14,673 of 21,858 hot experts
preloaded in 99–100 s, dev0 24.4/25.7 GB, asserted by the harness), cap=512,
80 completion tokens, same prompt suite:

| run | rotating median | warm-identical | cold | cold TTFT |
|---|---:|---:|---:|---:|
| gpu-real1 | 4.12 tok/s | 5.43 tok/s | 3.47 tok/s | 7.65 s |
| gpu-real2 | 4.13 tok/s | 5.44 tok/s | 3.47 tok/s | 7.48 s |

Delta on the headline (rotating median): |4.13 − 4.12| / 4.12 = **0.24%**,
against a 3% gate. **Pass.**

These sit where the steady-state table's patched 3-GPU row leads one to
expect — warm-identical 5.43 vs 5.35, cold 3.47 vs 3.59, cold TTFT 7.6 vs
7.1 — which is the corroboration that the tier really is up. The rotating
median is higher than that row's 3.69, consistent with the routing histogram
having grown since (21,858 hot experts now, against the 9,396→11,244 the
section above describes); that is the Q0 effect, measured here incidentally,
not a change in the engine. Note the same accumulation makes consecutive
runs not perfectly independent: `.coli_usage` is written back, so run N+1
preloads from a slightly richer history than run N.

> **Correction (same session, before any of this was relied on).** An earlier
> pair of runs (3.11 / 3.12 tok/s rotating, 0.32% apart) was written up here
> as "`qwen38-vk`, 3 GPUs". That label was wrong.
> The first `rome_bench.sh` set `OMP_*` and `COLI_VK_EXPERTS2/3` but never set
> `Q38_VULKAN=1`, and `qwen38_core.h:989` gates the whole GPU expert tier on
> that variable; `COLI_VK_DEV2`/`DEV3` gate dev2/dev3 on top of it, and unset
> means "don't try", not "auto" (`:1001`). So the tier never initialised and
> these are **CPU-path numbers from a Vulkan-capable binary, with zero GPUs
> in use**. The evidence is in the runs themselves: `datapoint.py` emitted no
> `| GPU |` row and no `| expert tiers |` row (it prints both whenever the
> engine reports them), and engine load was 8.70 s against the ~90 s the real
> 3-GPU preload costs per the steady-state section above. Those 3.11/3.12
> numbers belong against the patched-`qwen38` CPU line (2.89 rotating), not
> against a GPU row, and they are superseded by the verified table above.
> The harness now asserts the engine's own preload line and **refuses to
> record** a Vulkan config that comes up with an empty tier.

On cache state: the harness's own `echo 3 | sudo tee /proc/sys/vm/drop_caches`
needs interactive sudo on this box, so it warns and continues. That turns out
not to matter for the model shards, because `datapoint.py` evicts them itself
with `posix_fadvise(DONTNEED)`, which needs no privilege — and it labels the
row "cold request (cache evicted before engine load)" *only* when that
eviction succeeded, which is what these runs printed. So the `cold` column
above is a genuine cache-evicted cold start, and it lands where the
steady-state table's evicted cold rows do (3.47 vs 3.59). The sudo drop is
the broader whole-system eviction; the per-shard one is what the cold number
actually rests on. (An earlier revision of this section claimed the opposite —
that these were warm-cache runs — which was wrong.)

Two extraction bugs were found and fixed while building this harness, both
against the same class of mistake (a fixed `awk` field index into a table
whose column count differs by row): the rotating figure first read the
Decode table's "rotating 1" row instead of the Workload summary's median
(2.87/2.86 tok/s instead of the correct 3.29/3.27), and the cold figure read
the TTFT column instead of decode tok/s (13.00 instead of 2.29). Both are
fixed in the committed script; the gate1/gate2 numbers above are post-fix.

## G0: GLM-5.3 persistent-engine baseline

The steady-state section above only ever measured GLM as a fresh process
(3.17 tok/s). `rome_bench.sh glm53` gives the first persistent-engine numbers
(`SERVE=1`, expert/GPU state retained across requests, same regime as the
Qwen steady-state table): cap=512, 64 completion tokens, 8 threads, GPU tier
verified up (dev0 1296 heat-ranked from `.glm53_explain.bin`'s 7,754
candidates, dev2 1695, dev3 1695 — the documented count caps).

| run | rotating median | warm-identical | cold decode | cold TTFT |
|---|---:|---:|---:|---:|
| g0-run1 | 1.66 tok/s | 2.06 tok/s | 1.23 tok/s | 17.93 s |
| g0-run2 | 1.65 tok/s | 2.07 tok/s | 1.25 tok/s | 17.48 s |

Gate ("four numbers in the record"): **met.** Reproducibility: rotating
0.61%, warm-identical 0.48%, cold decode 1.6%, cold TTFT 2.5% apart.

Read against the fresh-process 3.17 tok/s: not comparable, and not a
regression. Fresh-process warms nothing and pays every first-touch cost once;
persistent measures four changing-prompt requests against a tier that has
already served 26–37 prompt tokens of cold-start overhead per request (TTFT
11.3–12.6 s inside rotating, RSS climbing 139→162 GB as new experts fault
in). This is exactly the regime the Qwen table shows a GPU tier struggling in
before G1/G2's fixes land (Qwen pristine `qwen38-vk` rotating was 2.72 against
a 5.34 warm-identical upper bound) — the same shape, unmeasured for GLM until
now. **This table, not the fresh-process number, is what G1 and G2 measure
against** ("rotating median and cold TTFT vs G0" / "tok/s vs G0").

## G1: prefault at bind and populate-at-load, ported to GLM — both regress, both shipped off

`glm53_populate_range()` is a straight port of `qwen38_core.h`'s
`q38_populate_range` (same `MADV_POPULATE_READ` call, page-aligned, advisory)
into `glm53.c`'s `expert_read` mmap fast path — the analogous per-bind
prefault — plus a second call site in `expert_map_init` that faults an
entire shard file at load instead of per-expert (the "populate-whole-mapping"
half of G1, which does not exist yet on the Qwen side either — that is
roadmap item Q0, still open — so this is a first implementation of the idea,
not a literal port of existing Qwen code).

**Oracle, both knobs, against the pristine binary** (CPU-only, same prompt,
`--logits`, so GPU residency cannot be a confound): `teacher_forcing`
bit-identical, `last_logits` max abs diff 0, cosine 1.0000000000, same
argmax. This only ever changes *when* a page faults, never what gets read,
so bit-identity is expected and confirms nothing in the read path moved.

**Performance, prefault at bind (`GLM53_MMAP_POPULATE`), through
`rome_bench.sh` against G0's exact config:**

| config | rotating median | cold decode |
|---|---:|---:|
| G0 (lazy fault) | 1.65–1.66 tok/s | 1.23–1.25 tok/s |
| prefault ON (2 runs) | 1.48, 1.48 tok/s | 0.98, 0.99 tok/s |
| prefault OFF, re-confirmed | 1.64 tok/s | 1.27 tok/s |

**Regression, not a gain** — the opposite of Qwen's 4–6× win from the same
primitive — and it reproduces: two runs at 1.48 each, and toggling only this
one env var with everything else fixed (same histogram copy, same cap,
same prompt suite) reproduces G0 when off and the regression when on. Root
cause was not profiled at the time (out of scope for a port), and two
hypotheses were guessed here: higher call frequency, or `mmap_lock`
contention between eight concurrent `madvise` callers.

> **Both guesses were wrong — see the G1b section below**, which instrumented
> it. The dominant cost was prefaulting experts that the GPU then serves from
> VRAM, whose host pages are never read (32% of binds). Contention was
> measured and ruled out. The paragraph above is left as written for the
> record; do not carry its hypotheses forward as findings.

**Performance, populate-whole-mapping-at-load (`GLM53_POPULATE_LOAD`):**
engine load 122.59 s (vs the usual ~9–10 s single-digit-second load — this
is the entire 182 GiB checkpoint being fault-in at once), for a rotating
median of 1.62 tok/s and cold TTFT of 17.66 s — both within G0's own
run-to-run noise (1.65–1.66, 17.48–17.93 s). **Correct, but no measured
benefit against a large, certain cost**: over 110 s of extra load latency
bought nothing detectable on either the metric it targets (cold TTFT) or
the primary one (rotating median). Not recommended as built; the request
mix here touches at most a few dozen of the checkpoint's 12,096 experts per
call, so eagerly faulting all of it may simply be solving a problem this
workload doesn't have at this scale.

**Shipped:** both knobs land as opt-in, default OFF — `GLM53_MMAP_POPULATE=1`
and `GLM53_POPULATE_LOAD=1` respectively, neither on by default. This is the
opposite default from Qwen's `Q38_MMAP_POPULATE` (default on there because it
measured a win); shipping GLM's version on by default would ship a regression
as the production default, which nothing in CLAUDE.md's discipline permits
without a favorable measurement.

**Gate ("teacher_forcing identical; rotating median and cold TTFT vs G0"):
met** — the oracle passed and both figures are measured against G0. The
Sonnet-tier premise for this item — "the design already exists in the repo,
the model copies and measures" — held for the mechanics (the primitive
ported cleanly, compiled clean, stayed numerically safe) but not for the
*expected direction* of the result: the pattern that worked for Qwen does
not transfer to GLM's access pattern. That is itself the measured answer G1
asked for.

## G1b: why the prefault regressed — measured, and it is not either hypothesis

G1 shipped its prefault off with two guesses on record (call frequency,
`mmap_lock` contention) and neither tested. Both are now tested. The answer is
a third thing, and the mechanism is fully quantified.

Instrumented `glm53_populate_range` with a per-call timer, a byte counter and
a bind classifier, reported through the existing `[PROF]` destructor. Fresh
process, 3 GPUs, same histogram copy, 32 greedy tokens, 8 threads:

| config | decode | populate calls | time in `madvise` | bytes advised | CPU expert compute |
|---|---:|---:|---:|---:|---:|
| prefault all (G1 as measured) | 2.410 tok/s | 60,684 | 14.394 thread-s | 143.41 GB | 3.393 s |
| prefault CPU-only (G1b fix) | 3.006 tok/s | 41,412 | 7.533 thread-s | 97.87 GB | 3.080 s |
| **prefault off (shipped)** | **3.222 tok/s** | 0 | 0 | 0 | 3.841 s |

**What it actually was.** `expert_read` binds an expert's slot
unconditionally, and `expert_mats` builds its views, *before* the MoE loop
decides whether to serve that expert from VRAM. For a GPU-resident expert the
host pages are then never touched at all — it is served from its
`vk_reg_at` tensors. The prefault was faulting in the full ~14 MB slot for
those anyway: **3,212 of 10,114 binds (32%), about 46 GB per 32-token
decode, for bytes nothing ever reads.** Skipping them is the fix, and it
recovers most of the regression (2.410 → 3.006 tok/s).

Two supporting facts from the same instrumentation. `contig=0`: **no expert
in this checkpoint is contiguous**, so every bind takes the six-`madvise`
path and the `ref->contig` single-call branch G1 wrote never once executes.
And the six pieces are ~2.36 MB each, so cost tracks bytes at roughly
0.3–0.4 µs per 4 KB page.

**Both original hypotheses are wrong, and one is disproved outright.** Call
frequency is a contributing factor only (6 calls/bind, a third of them
pointless) — fixing the pointless third left the primitive still net
negative. `mmap_lock` contention is **ruled out**: 172.2 µs/call at
`OMP_NUM_THREADS=1` against 181.9 µs/call at 8 threads. If concurrent
`madvise` were serializing on the mapping's lock, the single-threaded number
would be far lower. It is the same, so this is real page-table work, not
waiting.

**Why it still loses after the fix.** The mechanism does what it was designed
to do — it genuinely moves faults out of the matmul, CPU expert compute drops
3.841 → 3.080 s. It just spends 7.5 thread-seconds of `madvise` to save 0.76
thread-seconds of matmul: a ~10:1 losing trade. Eager population pays for
every page of the slot up front in a blocking syscall; the lazy path pays only
for what the arithmetic reaches, interleaved with the arithmetic.

**Kept, not removed** — deliberately against this record's own
"measured and rejected → code removed" precedent, for one reason: every fault
counted above is a *minor* fault, because this box holds the whole 182 GiB
checkpoint in page cache. That is this machine's defining and *unrepresentative*
property (see the top of this file). On a RAM-constrained box — the actual
deployment target for this work — those same first touches are major faults to
NVMe taken *inside* the matmul, which is precisely the stall Qwen's prefault
was built to remove. The sign of this knob is hardware-dependent and this rig
is the case where it loses. It stays `GLM53_MMAP_POPULATE=1`, off by default,
now strictly better than G1's version wherever it is switched on. Anyone
enabling it on a smaller box should re-measure there rather than trust either
number here.

The `[PROF]` instrumentation is permanent and free: the persistent-engine
rotating median with the knob off is 1.71 tok/s, matching G2's 1.71/1.69
exactly. It is GLM's first per-op visibility into the expert path
(mmap serve/copy, GPU vs CPU binds, populate cost) and feeds G3.

## G2: dev0/dev2/dev3 were sequential — issue-all/take-all fixes it, small real gain

**Check, first** (the roadmap's own first step): yes, sequential. GLM's MoE
dispatch called the three devices' *synchronous* forms back to back —
`coli_vk_expert_group` (dev0), `coli_vk_expert_group2` (dev2),
`coli_vk_expert_group3` (dev3) — each one submit-and-wait
(`coli_vk_expert_group` is `eg_prepare_submit` immediately followed by
`coli_vk_expert_group_take`, per `backend_vulkan.c`), so dev2's submit could
not start until dev0's fence had already signalled. The async `_issue`/`_take`
pair for all three devices already existed in `backend_vulkan.c` — built for
Qwen's `q38_moe_decode` — untouched and unused by `glm53.c` until now.

**Fix:** replaced the three sequential submit-and-wait loops with a
round-based issue-all-three/take-all-three loop (each device still chunks at
64 items per call — the descriptor-pool size `eg_prepare_submit` allocates
once — so a device needing more than one chunk still serializes against
itself across rounds; only same-round chunks across the three devices
overlap). The one invariant that had to hold under partial failure: a group
that was successfully issued is always taken before the loop can advance or
exit, win or lose, so a mid-round failure on one device can never leave
another device's single in-flight slot stuck for the next call.

**Oracle**, 3 GPUs, same resident set both runs (dev0 1296 heat-ranked, dev2
1695, dev3 1695 — verified identical via the preload lines before comparing):
pristine vs patched output files are **byte-for-byte identical** (`diff`
reports no difference at all — teacher_forcing and last_logits both, the
strongest form of this check).

**Performance**, through `rome_bench.sh` against G0:

| config | rotating median | cold decode |
|---|---:|---:|
| G0 | 1.65–1.66 tok/s | 1.23–1.25 tok/s |
| G2 (2 runs) | 1.71, 1.69 tok/s | 1.25, 1.27 tok/s |

A real, reproducible gain — every one of the four rotating requests improved
in both runs (1.63/1.63/1.79/1.80 and 1.63/1.63/1.76/1.82, against G0's
1.59/1.58/1.72/1.72) — but a modest one, roughly +3–4% on the headline, not
the 73% (2.37→0.63 ms/layer) Qwen measured from the identical restructuring.
Per-op timing to attribute this precisely (e.g. an `[VK_PROF]` or `[PROF]`
breakdown isolating the expert-group phase) was not pursued here — profiling
and its interpretation is Opus-tier (G3), and the direct before/after
tok/s answers this item's gate on its own. A plausible reason the win is
smaller than Qwen's, unconfirmed: GLM's per-token GPU-resident expert count
is usually small (topk routed across three devices), so each device's group
here is often just 1–2 experts — the three submits were already short, and
serializing three short things costs less than serializing three that are
individually larger, as Qwen's groups were.

Shipped unconditionally, no env knob — same choice as Qwen's version of this
fix: a pure concurrency reordering with an oracle-clean, no-regression
measurement doesn't need one.

**Gate ("teacher_forcing identical; tok/s vs G0"): met.** Unlike G1, this
port's expected direction held: the pattern that helped Qwen also helps GLM,
just by less.

## G3: where a GLM-5.3 decode token goes — and the roadmap has the lever wrong

The `[ATTN]` timer the roadmap names never existed in the tree (only in
`patch_attnsplit.py`, a session patch with a broken brace). This adds real
per-op timers to `run_layers`, `ffn_layer` and `forward_span`, gated on
`COLI_TIMERS=1` so the default path pays nothing, zeroed after prefill so a
`--greedy` run reports decode only; plus `perf` flat as the independent
cross-check. Fresh process, 3 GPUs (1296/1695/1695), same histogram copy,
8 threads pinned, prompt of 23 tokens, 128 greedy tokens. Fresh-process is
the right regime for a *split*; the absolute (373 ms/token here, 2.68 tok/s)
is not the serving number (585 ms at G2's 1.71 rotating), but every op below
runs identically in both.

### The table (decode only, 373.4 ms/token; layers-sum vs measured: 47.448 vs 47.451 s)

| op | ms/token | % | ms/call | calls/token | runs on | notes |
|---|---:|---:|---:|---:|---|---|
| **MoE FFN, of which:** | **199.2** | **53.4** | 4.74 | 42 | | fully accounted below (remainder 1.5 ms) |
| — CPU experts (`matmul_i4_grouped`) | 115.1 | 30.8 | 0.75/expert | 154 experts | 8 threads | 2.19 GB/token at **19 GB/s** against 70.9 GB/s DRAM at 8 threads: **ALU/decode-bound at 27% of bandwidth** — the same diagnosis the record made of Qwen's FP8 kernel before F16C |
| — router | 41.3 | 11.1 | 0.98 | 42 | **1 thread, scalar** | hand-rolled `for e<288: for d<4096: sum+=` in f32; a scalar reduction GCC will not vectorize; 4.7 MB/layer of f32 weights → 1.2 GMAC/s |
| — GPU expert groups (3 devices) | 21.7 | 5.8 | 0.52 | 42 | dev0/2/3 | CPU waits; does **not** overlap the CPU experts (they run before issue — the half of G2 deliberately deferred) |
| — shared expert | 19.6 | 5.3 | 0.47 | 42 | GPU dev0 | 3 dense submits; round-trip-dominated |
| — bind + dispatch + accumulate | 1.5 | 0.4 | | | | negligible now that prefault is off |
| **KDA (34 layers)** | **78.2** | **21.0** | 2.30 | 34 | **1 thread** | see decomposition below |
| **MLA / DSA (11 layers)** | **55.1** | **14.8** | 5.01 | 11 | **1 thread** | grew 4.10 → 5.01 ms/call as context went 87 → 151 tokens: O(context) |
| **mHC + RMSNorm (90 sites)** | **34.9** | **9.4** | 0.39 | 90 | 1 thread | `coli_hc_pre`: two `malloc`s per call plus a ~400k-MAC mix over the 16,384-float 4-stream residual, scalar (shared header with DeepSeek V4) |
| dense FFN (3 layers) | 3.2 | 0.9 | 1.08 | 3 | GPU | |
| lm_head | 2.8 | 0.8 | | 1 | | |

**perf flat, `cycles:u`, sampled from 25 s in so model load is excluded
(~4% of `quantize_loaded` still leaks; the timers, not perf, are the
authoritative split):** `libgomp.so` **60.76%**, `glm53` 32.31% (of which
`matmul_i4_grouped._omp_fn.0` 22.92%, `run_layers` 3.03% — `ffn_layer` and
`coli_hc_*` are inlined into it — `kda_layer` 1.23%, `mla_layer` 0.99%),
`libc` 4.14% (`memmove` 2.06%, `memset` 1.97%), `libm` 0.95%,
`libvulkan_radeon` 0.05%. The 60.76% is the OpenMP barrier spin of seven
threads while one works, and it is not noise: single-threaded or GPU-wait
wall time above sums to ~257 ms = 69% of the token, and 69% × 7/8 = 60%.
**The single largest fact in this profile is that 69% of the decode token
runs on one core.** Per-op perf shares of serial code are small (1–3%)
because perf counts cycles across 8 threads; the timers count wall.

### KDA, decomposed (2.30 ms/call, single thread)

State is 64 heads × 128 × 128 floats = 4 MB per layer, 136 MB across 34
layers; each token streams it twice, read and write, and it is cold from
DRAM every time because ~112 MB of expert weights pass through the caches
between visits to the same layer. That is 16 MB of traffic per call —
**0.9 ms at one core's measured 18.8 GB/s.** Then ~57k transcendental calls
per layer (`expf(alog[h])` recomputed 8,192 times per token though it has 64
distinct values; SiLU on 24,576 conv channels; the decay sigmoid on 8,192;
`expf` per k in the recurrence) ≈ **1 ms**, and 24,576 twelve-byte
`memmove`s per layer to shift the conv window (2.06% of all cycles) ≈
**0.4 ms**. 0.9 + 1 + 0.4 ≈ 2.3. The model closes.

Two of the roadmap's premises are wrong and one is moot. *"KDA ≈ 110 ms of
a 300 ms token":* it is 78 of 373, 21% not 37%, and third behind the CPU
expert kernel and (at this context) not far ahead of MLA. *"AVX2 vectorize
the scalar inner loop":* GCC's own `-fopt-info-vec` report shows both `for v`
passes in `coli_kda_step` already vectorized with 32-byte vectors; what it
cannot vectorize is the `expf` calls. *"per token × head × position over
L=512":* that loop is `mla_layer`'s absorbed-attention dot product over
`kv_lora = 512` — the roadmap describes MLA's inner loop and attributes it to
KDA. And *"KDA is bandwidth-bound, move it to the GPU":* at 2.3 ms against a
0.9 ms single-core floor and a 0.23 ms 8-core floor, it is not.

### The decision (the Fable gate after G3): CPU, not a shader

Parallelize `coli_kda_step` across its 64 independent heads (per-thread
`memory` scratch), hoist `expf(alog[h])` to once per head, and parallelize
the conv pass across its channels so the per-channel `memmove` spreads
eight ways. Not a ring index: `kda_window` is exported as a raw segment-
migration span, and a position counter would change its meaning. Expected
2.30 → ~0.35–0.45 ms/call, **78 → ~12 ms/token**, **bit-identical** (per-head
arithmetic order unchanged; the hoisted `expf` computes the same value once).
`delta_attention.h` turns out to be included by `glm53.c` alone — `kimi_k3`
has its own `kda_forward` — so there is no sibling oracle, only a rebuild. A GPU recurrence shader — none exists in
`backend_vulkan.c`; it is from-scratch — would floor at ~0.15–0.2 ms/call
(16 MB over VRAM bandwidth plus two ~55 µs submits, state resident in 136 MB
of VRAM), about 9 ms/token better than the CPU plan, for 2–4 days and a new
numerics path. Not now. The one-page spec with the oracle is
`G4-KDA-SPEC-2026-09-04.md`, next to this file.

### The ranked list this profile actually produces (fresh-process ms/token)

| # | lever | today | expected | oracle | tier |
|---|---|---:|---:|---|---|
| 1 | CPU int4 expert kernel at 27% of DRAM bandwidth | 115 | −60 to −80 | teacher_forcing + last-logits vs pristine | Opus (kernel) |
| 2 | KDA: expf hoist + head-parallel + conv-channel-parallel | 78 | −65 | bit-identical | Sonnet, spec + procedure written |
| 3 | MLA: parallelize the 64-head loops | 55 | −45 at 151 tokens, more at length | bit-identical per head | Sonnet |
| 4 | router: parallelize across the 288 rows | 41 | −36 | bit-identical (row order kept) | Sonnet, trivial |
| 5 | mHC: drop the per-call mallocs, vectorize/parallelize the mix | 35 | −28 | bit-identical if order kept; shared header — rebuild DeepSeek V4 too | Sonnet |
| 6 | overlap CPU experts with the in-flight GPU groups (G2's deferred half) | 22 | −22 | bit-identical | Sonnet |

Items 2–6 are ~−195 ms of 373 with no new kernel and no numerics change;
item 1 is the only kernel. Together they point at ~100–180 ms/token, i.e.
**5.5–10 tok/s fresh-process against 2.7 today**, versus the track's stated
target of "about 4 tok/s" from a 2–4 day KDA shader. G4 as written is
superseded by the spec; the roadmap rows are updated to match.

The instrumentation ships gated: the harness exports `COLI_TIMERS=1`, so
its row below (`g3-timers-on`) is the proof it costs nothing in the serving
regime.

## G4: KDA parallelised — +7% serving, and the spec's floor was wrong

Executed `G4-KDA-SPEC-2026-09-04.md` in three commits. Result: **rotating
median 1.84 / 1.83 tok/s against G2's 1.69–1.71 (+7–8%)**, everything
bit-identical. The spec's per-call gate was not met, and the reason is a
mistake in G3's decomposition that only a sub-timer could find.

| step | change | `[OPTIME] kda` ms/call | oracle |
|---|---|---:|---|
| — | G3 baseline | 2.301 | — |
| 1 | `expf(A_log[h])` hoisted out of the `d` loop (8,192 → 64 calls/token) | 2.249 | bit-identical |
| 2 | 64 heads in parallel, per-thread `memory` scratch | 2.207 | bit-identical |
| 3 | 24,576 conv channels in parallel | **1.598** | bit-identical |

**Step 2 fired the stop condition and that was the point.** Parallelising
the heads — the spec's centrepiece, predicted to reach ~0.5 ms/call — moved
2%. The ROADMAP said to stop there and re-examine rather than pile on more
parallelism, so `kda_layer` was sub-timed (8 threads, greedy 32):

| part | 8 threads | 1 thread | scales? |
|---|---:|---:|---|
| projections (8 matrices, one GPU submit) | 0.660 ms | 0.886 | 1.3× |
| decay | 0.062 | 0.061 | no |
| `coli_kda_step` | 1.031 | 1.060 | **no — 3%** |
| output norm | 0.064 | 0.063 | no |
| `ko` (one more GPU matvec) | 0.400 | 0.489 | 1.2× |

Two errors in §G3 show up here. First, `coli_kda_step` was flat under head
parallelism because its *other* loop — 3·width = 24,576 conv channels, one
`expf` each — was still serial; step 3 took that 1.031 → 0.378 ms (2.7×),
and it, not the head loop, is where KDA's CPU time lived. Second and larger:
**projections + `ko` are 1.05 ms, ~47% of the original 2.22, and they are GPU
submit round trips** — two per token per layer, 9 matrices, ~55–66 µs each
plus the matvecs. §G3 decomposed KDA as 0.9 ms DRAM + 1.0 transcendentals +
0.4 memmove ≈ 2.3 and got the total right by coincidence while attributing
none of it to the dense projections around the recurrence.

So the spec's gate (≤ 0.6 ms/call) was never reachable by parallelising the
recurrence: **the floor for this approach is ~1.05 ms/call**, and at 1.598
the remaining CPU-side slack is ~0.54 ms (step 0.378 + norm 0.097 + decay
0.060). Halving KDA again needs the *submits* addressed — chaining `ko` into
the same batch as the eight projections would remove one of the two round
trips per layer per token — not more threads.

Fresh-process: 78 → 54 ms/token of a 373 ms token. Serving:

| run | rotating median | cold decode | cold TTFT |
|---|---:|---:|---:|
| g4-run1 | 1.84 tok/s | — | — |
| g4-run2 | 1.83 tok/s | 1.36 tok/s | 16.07 s |

against G0's 1.65–1.66 / 1.23–1.25 / 17.5–17.9 and G2's 1.69–1.71. The
serving delta (+0.13 tok/s ≈ +7.5%) is larger than the fresh-process ms
arithmetic predicts (−24 ms of 585 ms ≈ 4%), consistent with the freed
cores also relieving the barrier spin §G3 measured at 60.8% of cycles.

`kimi_k3` and `qwen36` do not include `delta_attention.h` (verified in
§"Executing G4"); both rebuilt clean anyway.

**Next for KDA, if it is picked up again:** fold `ko` into the projection
batch (one submit per layer instead of two, ~0.4 ms/call, bit-identical),
and only then reconsider anything on the GPU. The ranked list below is
unchanged otherwise — the router at 41 ms/token is still the cheapest
remaining item.

## Long context: the indexer is real, and it is not the long-context problem

G5's gate asked for a DSA-index timer GLM did not have. Added one (`[OPTIME]
mla split`: projections / indexer / attention core), then measured decode at
two context lengths — which is what the ordering of G5 against G8 had been
resting on guesswork for.

| context | proj | **index** | attn | mla total | index per ctx token |
|---:|---:|---:|---:|---:|---:|
| 52 | 1.049 ms | **0.108 ms** | 2.901 ms | 4.060 ms | 2.104 µs |
| 502 | 1.064 ms | **1.050 ms** | 15.534 ms | 17.651 ms | 2.090 µs |

**The indexer is exactly linear per call** — 2.09 µs per token of context,
constant across a 10× range — so it is O(context) per token and O(context²)
per generation. G5's premise is confirmed.

**But the attention core is bounded and the indexer is not**, and that is the
part the roadmap had backwards. From the code: `coli_sparse_index_width()`
returns `topk + pool - 1` = **2051, a constant**, and the attention loop runs
over that width, skipping entries past the end. The pooling loop runs over
`pools = (sequence + pool - 1) / pool`, which grows forever. So:

- `attn` ≈ 1.44 ms + 28.07 µs × min(ctx, 2051) — **flat above ~2k context**
- `index` ≈ 2.09 µs × ctx — **never flattens**

Both are single-threaded. Per token (11 MLA layers), holding the fits:

| context | index ms/token | attn ms/token | index share of MLA |
|---:|---:|---:|---:|
| 2k | 46 | 650 | 7% |
| 8k | 188 | 650 | 22% |
| 16k | 367 | 650 | 36% |
| 32k | 735 | 650 | 53% |

So the indexer only overtakes the attention core somewhere near **27k tokens**
of context. Below that, the biggest long-context cost is `attn` — flat, large,
and single-threaded, which is **G8**, not G5. The roadmap's "the only
O(context²) component" is true and was being read as "the long-context fix";
it is not, at any context anyone is likely to run here.

**This also sets G5's real trigger.** G8 parallelises `attn` across 8 cores
(650 → ~81 ms/token). After that, the indexer is MLA's dominant term above
roughly **3.5k context**, not 27k. G8 first, then G5 — and G5's value is a
function of the context length the deployment actually sees, which is a
product question this record cannot answer.

Caveat on the fits: two points, at 52 and 502 tokens, both below the 2051
cap, so the flattening above it is derived from the code's constant bound and
not yet measured. A run at ~4k would confirm it; the 1.9k and 3.8k runs were
started and stopped because a prefill that long costs more rig time than the
derivation is worth. If `attn` does *not* flatten near 2k, this whole ranking
inverts and G5 moves ahead of G8.

## G7: the router, parallelised — the first leg of the post-G4 road

First item on the road ranked in the record's long-context section. The
288-row MoE router score is a scalar f32 reduction against the current
token, single-threaded; each row is independent (own weight row, own
`score[e]`, no shared accumulator), so `#pragma omp parallel for` on the
expert loop changes nothing about any row's own summation, only the order
rows are computed in.

**Caught before it reached the rig:** the first draft put the pragma on the
outer per-token loop instead of the inner per-expert loop. `score` is one
buffer reused across tokens with no per-token dimension, so that would have
had different threads write the same buffer for different tokens during
prefill — silent corruption, not a crash. Fixed by inspection before building.

**Oracle**, 3 GPUs, same histogram, teacher_forcing and last_logits by
`diff`, no tolerance: standard prompt and ~700-token prompt both
**bit-identical**.

**`[OPTIME] moe split: router`**, greedy 128, 8 threads:

| | ms/call | vs G4 |
|---|---:|---:|
| pristine (serial) | 0.998 | — |
| parallel | **0.137** | 7.3× (91% of the theoretical 8×) |

−0.861 ms/call × 42 MoE layers/token = **−36.2 ms/token**, matching the
road's −36 ms estimate almost exactly. Fresh-process decode 2.953 → 3.533
tok/s (+19.6%), greedy 128.

**Serving gate**, `rome_bench.sh`, against G4's 1.84/1.83:

| run | rotating median |
|---|---:|
| g7-run1 | 2.01 tok/s |
| g7-run2 | 2.05 tok/s |

+9–12%, again outrunning the raw ms arithmetic — same pattern as G4: freeing
single-threaded work also relieves the barrier spin G3 measured at 60.8% of
cycles, which a per-op ms sum does not capture. **Gate met.**

## G8: MLA's two head loops, parallelised — second leg of the road

The record's own long-context section named MLA's attention core as the
largest single long-context cost (~650 ms/token flat above 2k). Second on
the road for that reason, ahead of G5.

**Checked before writing anything:** `mla_layer`'s two per-head loops (the
absorb projection, and the attention-core softmax+pool+`kvb_v`) already call
`mv_rows` (`quant.h`), which has its own `#pragma omp parallel for` over its
own output rows. Naively wrapping the outer per-head loop would nest one
parallel region inside another — exactly the class of regression already on
record for this box (`num_threads(8)` inside a 16-thread pool, Qwen: 1.70 vs
1.90 tok/s). Rather than assume either way, compiled a 20-line probe with
the exact same `gcc -O3 -fopenmp` flags this codebase uses and measured
`omp_get_max_threads()` inside a nested region directly on the rig:

```
omp_get_nested()            = 0
omp_get_max_active_levels() = 1
threads observed inside the nested region = 1
```

Nesting collapses to one thread per call by default on this box's
GCC/libgomp. Wrapping the outer loop is safe — `mv_rows`'s inner pragma
silently becomes a no-op serial loop when called from inside another
parallel region. This also explains G3's own number: today the code pays
**two tiny OMP team spin-ups per head per token (128 total)**, each barely
using its 8 threads before tearing back down, rather than one team doing
real work across 64 heads at once.

**The absorb loop** was embarrassingly parallel as found — each head writes
its own `absorbed[]` slice, reads its own `queries[]` slice, no shared
mutable state.

**The attention-core loop** had the same trap G4's KDA fix guarded against:
`score[]` and `pooled[]` were single buffers reused across all 64 heads.
Sized per-thread (reusing `coli_kda_threads()` from `delta_attention.h`,
already included, already carrying the `#ifdef _OPENMP` this needs) and
indexed by `omp_get_thread_num()`. A shared slice would have corrupted every
head's softmax while still emitting plausible tokens.

**Oracle**, 3 GPUs, same histogram, teacher_forcing and last_logits by
`diff`, no tolerance: standard prompt and ~700-token prompt both
**bit-identical**.

**`[OPTIME] mla split`**, greedy 128, mean context 88 tokens:

| phase | before | after | |
|---|---:|---:|---|
| proj (absorb) | 1.024 ms | 0.659 ms | 1.55× |
| attn (core) | 3.806 ms | 1.237 ms | 3.08× |
| index (untouched) | 0.180 ms | 0.168 ms | — |
| **total** | **5.013 ms** | **2.066 ms** | **2.43×** |

−2.947 ms/call × 11 MLA layers/token = **−32.4 ms/token** at this context.
Fresh-process decode 3.383 → 3.985 tok/s (+17.8%), greedy 128.

**Correction to the commit message** (`c/glm53.c`, this item): it states both
improved phases are "context-independent... flat across context length."
That is right for `proj` (each head's absorb is a fixed 512-wide matmul
regardless of context) and wrong for `attn`: the long-context section above
already modeled `attn ≈ 1.44 ms + 28.07 µs × min(ctx, 2051)` — it grows with
context up to the indexer's 2051-token cap, then plateaus, same as the
underlying cost G8 is parallelising. So G8's *relative* speedup (3.08× on
`attn`) should hold at any context, but its *absolute* ms saved grows up to
~2051 tokens before flattening, same shape as the term itself. This does not
change why G8 was placed ahead of G5 — the attention core is still the
larger of the two below ~27k tokens per that section's own numbers — only
the claim that G8's saving is flat regardless of context, which it is not
below the cap. Not re-measured at long context here; the shape follows from
the model already on record, not a new measurement.

**Serving gate**, `rome_bench.sh`, against G7's 2.01/2.05:

| run | rotating median |
|---|---:|
| g8-run1 | 2.19 tok/s |
| g8-run2 | 2.17 tok/s |

+6–9%, again ahead of the raw ms arithmetic — same pattern as G4 and G7.
**Gate met.**

## G9: CPU expert compute overlapped with the in-flight GPU groups — third leg

G2 left this half deliberately deferred: its own record entry above says so.
Third on the road because it falls straight out of G2's sub-split rather
than needing new evidence — G3's `[PROF] eg`/`cpu` numbers already showed
the CPU experts finishing entirely before the GPU groups were issued.

**Mechanism.** In `ffn_layer`'s classify loop, a CPU-only expert's `Mat`
triple (`gate`,`up`,`down`) and `eid` are now saved into a deferred list
instead of being computed immediately. A new helper,
`ffn_moe_run_deferred_cpu`, runs that saved list once, in the gap between
the GPU groups' issue and take (whichever round issues anything first) —
so the CPU compute overlaps the fence wait instead of finishing before it
starts. A catch-all placed right after the dispatch block's closing brace
runs any still-undone deferred experts for a layer where nothing routed
anywhere GPU-resident (so the issue/take block never executed at all).

**Safety, checked before writing anything:** deferring is gated on
`can_defer = g_vk_ready && (n_union <= block)`. The block-based expert
cache exists so a later block's `expert_read` can evict an earlier block's
slots once that block's own experts are consumed — and a deferred CPU
expert's saved `Mat` still points into its slot's weight buffer, so
deferring past the classify loop is only safe if no eviction can happen
between classifying and running it. `n_union <= block` (`cache->cap` is
512 in every measured config; `n_union <= topk*tokens` for decode) means
exactly one block, so there is no later block to evict anything —
decode always qualifies. A smaller cap or a larger prefill batch falls
back to today's immediate-compute path, completely untouched.

**Does not change summation order into `out[]`:** CPU contributions still
land in the same relative order they always did — the order experts first
appear in `union_ids`, still entirely before the GPU dev0/1/2
contributions. Only the wall-clock moment moved, not the arithmetic.

**Oracle**, vs `65d21e2` (G8, the immediately preceding landed commit), 3
GPUs, same histogram, teacher_forcing and last_logits by `diff`, no
tolerance: standard prompt and ~700-token prompt both **bit-identical**.

**`[PROF]`**, greedy 128:

| | eg (GPU groups) | cpu (CPU experts) | serial total |
|---|---:|---:|---:|
| before | 2.637 s | 13.612 s | 16.249 s |
| after | 11.749 s | 11.193 s | — |

`cpu`'s own duration is essentially unchanged (11.193s vs 13.612s is the
same work, not a speedup) — what changed is that `eg`'s timed window now
*contains* it instead of following it, so the two numbers are no longer
additive. `eg` after (11.749 s) sits almost exactly at `cpu`'s own duration
rather than at the old sum: the ~2.6 s of pure GPU wait is nearly entirely
hidden behind CPU compute that was already larger than it. Unlike G7/G8,
this is not a clean before/after of the same phase boundary — `[PROF] eg`'s
semantic scope changed by design, which is the gate criterion itself
(`[PROF] eg` no longer serial with `cpu`), not a regression in `eg` alone.

Fresh-process decode 3.741 → 4.237 tok/s (+13.3%), greedy 128, better than
the roadmap's −22 ms/token estimate (that estimate assumed only partial
hiding; the GPU wait turned out to be smaller than the CPU work it now
hides inside, so the hiding is close to total).

**Serving gate**, `rome_bench.sh`, against G8's 2.19/2.17:

| run | rotating median |
|---|---:|
| g9-run1 | 2.33 tok/s |
| g9-run2 | 2.35 tok/s |

+7–8%, ahead of the raw ms arithmetic again — same pattern as G4, G7, G8.
**Gate met.**

## G12: `ko`'s round trip cannot fold into the projections' submit — blocked

Checked before writing anything, per this record's own practice, and the
premise does not survive: G12 as specified ("fold `ko` into the same
batched submit as the eight projections, two round trips become one") is
**not achievable as a Sonnet-tier plumbing change.**

**Why the literal fold is impossible.** `ko`'s input is `normed`, not the
row the eight projections read. `normed` only exists after
`coli_kda_step` (the CPU recurrence) and the per-head norm+gate step run —
both entirely on CPU, both strictly between the projections and `ko`,
within the same token, same layer. Putting `ko` in the same
`vkQueueSubmit` as the projections needs that CPU stage to also run on the
GPU, as a barrier-separated stage in one command buffer — a real compute
shader for the recurrence, not a port of the existing `vk_batch_mv_chain`
pattern.

**Why the G9-style workaround (fold with independent work instead) doesn't
apply either.** G9 worked because the CPU-only experts were provably
independent of the GPU expert groups within the same MoE layer. Checked
whether the same trick applies here — it doesn't, because GLM-5.3 decode is
strictly sequential at both granularities:

- `kda_layer`'s `tokens` parameter is **1 during decode** (confirmed: this
  track's own numbers only reconcile if it is — `[OPTIME] kda`'s ms/call
  divided into ms/token gives ~34 calls per token, matching the KDA layer
  count, not a token batch). There is no "next token" inside one call to
  pipeline `ko(t)` against.
- Each layer's output feeds the next layer's input through the residual
  stream (`coli_hc_post`/`branch`/`next`, `glm53.c` main layer loop) — so
  there is no independent GPU work anywhere else in the same decode step
  to overlap `ko`'s wait with, unlike G9's CPU experts.

**Checked, and ruled out, the one alternative that would have been pure
plumbing:** whether `ko`'s separate dense-matmul call (`coli_vk_matmul`,
the singular path) pays a full descriptor-rebind + command-buffer
re-record cost every call because the interleaved batched-projections path
(`coli_vk_matmul_multi`, which shares `G.cmd`/`G.fence`/`G.dset` and
explicitly invalidates the singular path's cache — `backend_vulkan.c`,
`G.cmd_ready = 0; G.bound_tensor = NULL;` after every multi-batch call, by
design) forces the expensive path every time. A `VK_PROF=1` diagnostic run
confirms this is **not** the cost:

Standard 3-GPU environment, `--greedy 260`, cumulative at n=49152 calls
(aggregate across all non-batched dense calls, not `ko`-isolated, but the
same shared code path):

| phase | µs/call |
|---|---:|
| descriptor rebind | 0.61 |
| command-buffer re-record | 1.24 |
| **submit** | **23.1** |
| **fence wait** | **220.7** |
| memcpy x / y | 4.46 / 2.71 |

Rebind and re-record are negligible. The real cost is exactly what the
roadmap's own evidence line said — genuine submit+fence round-trip
cost — and the only way to remove it is to remove a whole round trip,
which needs the recurrence on the GPU.

**Verdict: blocked as a Sonnet item.** Achievable only as a new GPU kernel
(the recurrence and/or the norm+gate step moved to a compute shader,
enabling a real single fused submit) — Opus tier, and the same class of
premise check that sent the original KDA plan to the Fable gate after G3.
No code changed; nothing shipped.

**Decision: skip, don't escalate to Fable now — this is not a new
question.** `G4-KDA-SPEC-2026-09-04.md` §"Why not the shader, in numbers"
already priced a GPU recurrence kernel at the original Fable gate: floor
~6 ms/token against the CPU plan's ~15, i.e. **~9 ms/token for 2–4 days
and a second numerics path**, and set an explicit trigger, verbatim:
**"Revisit only after items 1–6 of the G3 ranked list are done, if KDA is
then the largest remaining bucket."** G12's fold needs essentially that
same kernel (the recurrence and/or norm+gate on GPU) to let `ko` share a
submit, for a similar-sized payoff (~14 ms/token) — so asking Fable to
re-decide this now would be re-litigating an already-answered question,
not a fresh one, and the trigger condition for reopening it hasn't even
been reached: items 1–6 of the G3 list are G11, G4, G8, G7, G10, G9 — G10
and G11 are still not started. **Skip to G10; the post-G9/G10 re-profile
milestone (already on this roadmap) is the natural point to check whether
KDA has become the largest bucket, which is the one thing that would
justify reopening this with Fable.** G12 stays blocked, not abandoned —
its answer is tied to the same trigger as the original shader question.

## G10: mHC's pre/post, parallelised — the malloc removal did not survive contact

`hyper_connections.h` is shared with DeepSeek V4 (rebuilt clean; not
re-measured — no rig checkpoint, out of this track), so every change here
had to stay safe for a caller this record has never run.

**The parallelisable part.** `coli_hc_pre`'s mix computation is
`mix_count` independent dot products (24 rows at `hc_mult=4`, ~400k MACs
total) against the same input row — parallelised over rows, each row's
own scalar reduction untouched, same reasoning as G7's router fix, not a
SIMD reduction reorder. `coli_hc_pre`'s final collapse (`dimension`
columns, hc-term reduction each) and `coli_hc_post`'s destination×column
mix (`collapse(2)`, hc-term reduction each) parallelise the same way.
Nesting safety already proven by G8's compiled `max_active_levels` probe.

**The malloc removal did not survive contact.** The roadmap also asked to
drop `coli_hc_pre`'s two per-call `malloc`s (mixes, pre — 90 sites/token).
Tried it: since `hc_mult` is bounded to 8 by every loader, replaced both
with fixed-size stack arrays. **Not numerics-neutral on this box.** Once
`hc`'s range is knowable at compile time — whether from the fixed array
size itself or from an explicit `hc <= 8` guard added defensively — GCC
produces different rounding in the small hc-bounded reductions, changing
`last_logits` by ~1.9e-4 absolute (cosine 0.9999999999258, argmax and
`teacher_forcing` both unaffected) with zero change to the source's
logical operation order.

Bisected properly before accepting or rejecting anything: tried
`__attribute__((optimize("no-tree-vectorize")))` (no effect — same exact
delta), `-ffp-contract=off` (changed the delta to a *different* nonzero
value, ruling out a simple on/off toggle), `#pragma GCC unroll 1` on every
hc-bounded loop (no effect), an `asm volatile` aliasing barrier after each
array's declaration (no effect), and `__attribute__((noipa))` (no effect).
None restored bit-identity. Reverting *only* the mallocs — keeping every
`#pragma omp` — gave `max_abs=0.0` directly: the parallelism was never the
problem. **Verdict: keep the mallocs, ship the parallelism.** The malloc
removal was the small piece of this item, not the point of it, and
CLAUDE.md's rule is unconditional here — no numerics change ships
without an env knob and a measured diff in the commit body, and this one
isn't worth either for ~90 tiny allocations/token.

**Oracle**, vs `da99b9c` (G9), 3 GPUs, same histogram, teacher_forcing and
last_logits by `diff`, no tolerance: short prompt **bit-identical**
(`max_abs=0.0`); ~700-token prompt (1260 prefill positions) **bit-identical**
(`max_abs=0.0`, `teacher_forcing` identical across all 1260 positions).

**`[OPTIME] hc+norm`**, greedy 128 (n=11520 calls, 90 sites/token):

| | before | after | |
|---|---:|---:|---|
| ms/site | 0.393 | 0.097 | 4.05× |

−0.296 ms/site × 90 sites/token = **−26.6 ms/token**, close to the
roadmap's −28 ms/token estimate. Fresh-process decode 4.917 → 6.157 tok/s
(+25.2%), greedy 128 — the gap over the raw `hc+norm` arithmetic (which
alone predicts ~+13%) is the same freed-barrier-spin pattern as G4/G7/G8/G9.

**Serving gate**, `rome_bench.sh`, against G9's 2.33/2.35:

| run | rotating median |
|---|---:|
| g10-run1 | 2.60 tok/s |
| g10-run2 | 2.75 tok/s |

+11–17%, well ahead of the raw ms arithmetic (~+13%) — same pattern as
every parallelisation item on this track. **Gate met.**

## RP1: re-profile after G4/G7/G8/G9/G10 — the arithmetic held, the bottleneck changed shape

The milestone the re-measurement cadence schedules "before G11". Same
method as §G3 so the two tables are comparable: fresh process, 3 GPUs,
tier **1296/1695/1695 (identical to G3)**, 8 threads pinned, `COLI_TIMERS=1`
for the authoritative split, `perf` flat as the independent cross-check,
`--greedy 128`. Two differences from G3, both stated rather than hidden:
the prompt is 42 tokens not 23 (mean ctx **106**, not 87 — MLA and the
indexer are measured slightly *long* here), and the routing histogram has
two more days of history in it, which matters more than anything else
below.

### The table (decode only, 199.4 ms/token; layers-sum + head vs measured: 25.524 vs 25.524 s, closing to 0.04 ms/token)

| bucket | G3 ms/token | now ms/token | Δ | G3 % | now % |
|---|---:|---:|---:|---:|---:|
| **MoE FFN total** | **199.2** | **105.4** | −93.8 | 53.3 | 52.9 |
| — CPU int4 experts | 115.1 | **74.8** | −40.3 | 30.8 | **37.5** |
| — router (G7) | 41.3 | 5.8 | −35.5 | 11.1 | 2.9 |
| — shared expert | 19.6 | 15.0 | −4.6 | 5.2 | 7.5 |
| — GPU expert groups | 21.7 | 8.6 | −13.1 | 5.8 | 4.3 |
| — bind + dispatch + accumulate | 1.5 | 1.2 | −0.3 | 0.4 | 0.6 |
| **KDA (34 layers)** (G4) | **78.2** | **55.0** | −23.2 | 20.9 | 27.6 |
| **MLA / DSA (11 layers)** (G8) | **55.1** | **24.3** | −30.8 | 14.8 | 12.2 |
| **mHC + RMSNorm (90 sites)** (G10) | **34.9** | **8.9** | −26.0 | 9.3 | 4.5 |
| dense FFN | 3.2 | 2.9 | −0.3 | 0.9 | 1.5 |
| lm_head | 2.8 | 2.8 | 0.0 | 0.7 | 1.4 |
| **total** | **373.4** | **199.4** | **−174.0** | | **1.87×** |

Sub-splits: KDA per call — proj 0.661, decay 0.061, step 0.385, norm 0.103,
`ko` 0.405 ms. MLA per call (ctx 106) — proj 0.642, index 0.208, attn 1.358 ms.

### Where the 174 ms came from — and 23.5 of it was not us

Each landed item measured its own delta against its own immediately
preceding baseline, with the histogram frozen inside each pair:
G4 −24.0, G7 −36.2, G8 −32.4, G9 −31.25, G10 −26.6 = **−150.4 ms/token**.
373.4 − 150.4 = **222.9**, which is exactly the ~223 ms/token the roadmap
was projecting. Measured today: **199.4**. The missing 23.5 ms/token is
**the expert tier hitting better, not code**:

| | CPU experts/token | GPU experts/token | GPU share |
|---|---:|---:|---:|
| G3 | 154.0 | ~182 | 54% |
| now | 69.6 | 266.4 | **79%** |

Same tier *size* (1296/1695/1695), same 336 activations/token (42 MoE
layers × topk 8) — different tier *contents*, because
`~/.glm53_explain.bin` has accumulated every bench run since Sep 4 and the
heat ranking now matches the real routing much better. **The road's own
arithmetic was right; the extra came free from a warmer histogram, and it
will not repeat.** Any projection built on today's 199.4 inherits that.

### perf flat, `cycles:u`, decode only (sampled by attaching after prefill, 153k samples over 20 s)

| DSO | G3 | now |
|---|---:|---:|
| `libgomp.so` | 60.76% | **56.35%** |
| `glm53` | 32.31% | **41.44%** |
| `libc` | 4.14% | 0.44% |
| `libm` | 0.95% | 0.84% |
| `libvulkan_radeon` | 0.05% | 0.07% |

Top symbols now: `matmul_i4_grouped._omp_fn.0` **24.06%**,
`coli_kda_step._omp_fn.1` 4.53%, `mla_layer._omp_fn.1` 3.55%,
`ffn_layer._omp_fn.0` (router) 3.38%, `coli_hc_pre._omp_fn.0` 2.56%,
`coli_kda_step._omp_fn.0` 1.59%, `coli_hc_post._omp_fn.0` 0.67%,
`kda_layer` (serial) 0.33%, `mla_layer` (serial) 0.15%,
`run_layers` 0.21%, `memmove` 0.34%. Every hot symbol is now an `_omp_fn`;
at G3 the serial `run_layers` alone was 3.03% and `memmove` 2.06%.

**G3's model of the libgomp share stopped working, and that is the
finding.** G3 explained 60.76% as seven threads spinning while one worked:
69% single-threaded × 7/8 = 60%, and it matched to within a point. Today
the single-threaded-or-GPU-wait share is **41%** (81.7 of 199.4 ms/token),
so the same model predicts 36% — but perf measures **56.35%**, a 21-point
excess.

### The spin is load-bearing, not waste

The obvious reading of "56% of cycles in libgomp" is that it is recoverable.
It is not. Same config, only the OpenMP wait policy changed:

| config | tok/s | ms/token | `hc+norm` ms/site | `kda` ms/call | `mla` ms/call |
|---|---:|---:|---:|---:|---:|
| default (spin) | **5.011** | **199.4** | 0.099 | 1.617 | 2.211 |
| `OMP_WAIT_POLICY=passive` | 4.664 | 214.4 | 0.191 | 1.740 | 2.668 |
| `GOMP_SPINCOUNT=1000` | 4.827 | 207.2 | 0.141 | 1.728 | 2.647 |

Sleeping instead of spinning costs **7.5%**, and it costs the *shortest*
region most — `hc+norm` nearly doubles (0.099 → 0.191 ms/site) while the
longest bucket moves least. The campaign replaced a few long serial
stretches with roughly **476 short parallel regions per token**
(~209 `matmul_i4_grouped` from 69.6 experts × 3 matmuls, 68 KDA, 90 mHC-pre,
45 mHC-post, 42 router, 22 MLA), and the aggressive spin is what makes
regions that short viable at all. `perf`'s call graph cannot split the spin
per region — libgomp's workers wait in the thread pool, not on the caller's
stack (13.16% inclusive under `start_thread`, no user frames above it) — so
the wait-policy experiment above is the causal evidence, not the unwinder.

**The lever this leaves is fewer regions and fewer round trips, not less
spinning and not more parallelism.**

### What this changes for G11 — its bucket shrank and its diagnosis moved

G3 sized G11 from a 115.1 ms/token bucket streaming 2.19 GB/token at
19 GB/s, 27% of this box's 70.9 GB/s. Recomputed today from the same
14.2 MB expert slot the loader reports:

| | experts/token | GB/token | ms/token | effective | of DRAM |
|---|---:|---:|---:|---:|---:|
| G3 | 154.0 | 2.19 | 115.1 | 19.0 GB/s | 27% |
| now | 69.6 | 0.99 | 74.8 | **13.2 GB/s** | **19%** |

Two things follow, and they pull in opposite directions:

1. **The roadmap's "−60 to −80 ms/token" for G11 is no longer reachable.**
   The whole bucket is 74.8 ms/token. Even a kernel that became free
   cannot pay out 80. The estimate needs rewriting against 74.8, not 115.1.
2. **The kernel got *less* efficient, and G9 is why.** 19 → 13.2 GB/s is
   not a regression to fix in isolation: G9 deliberately made this kernel
   run concurrently with GPU DMA and the issue/take thread, so it now
   shares DRAM bandwidth and gives up a core. G9 still won its gate
   (+7–8% serving); the per-kernel rate is the price paid for the overlap.

And a third, new, structural fact — **inside the CPU-expert window only
57% of core-time is in the kernel.** The window is 37.5% of wall
(74.8 of 199.4 ms/token), so at 8 busy cores it should command ~37.5% of
cycles; perf measures `matmul_i4_grouped` at 24.06%. About 12 of the
missing ~43 points are thread 0 doing G9's issue/take by design; the rest
is the serial `swiglu_clamped` and the per-token accumulate sitting between
each expert's three parallel matmuls (`mlp3_cpu`: `mv` ∥, `mv` ∥,
**swiglu serial**, `mv` ∥, then **accumulate serial**), plus the barrier
cost of three short regions per expert. **A G11 that only tunes the inner
kernel leaves that on the table; the expert *path* is as much the target as
the kernel.**

### GPU round trips are now the second structure, at 29% of the token

| site | ms/token |
|---|---:|
| KDA proj (8 batched projections) | 22.5 |
| KDA `ko` (the one G12 could not fold) | 13.8 |
| shared expert (3 dense submits) | 15.0 |
| MLA proj (5 batched projections) | 7.1 |
| **total** | **58.4** (29%) |

At G3 this was ~41 ms of 373 (11%). It is now 29% of a much shorter token.
**This strengthens the case §G12 deferred** — G12 was blocked because
folding `ko` needs the KDA recurrence on the GPU, priced at the original
Fable gate as ~9 ms/token for 2–4 days when KDA was 78 ms of a 373 ms
token. The trigger written into `G4-KDA-SPEC-2026-09-04.md` was "revisit
only after items 1–6 of the G3 list are done, if KDA is then the largest
remaining bucket". KDA is **not** the largest (55.0 vs the CPU experts'
74.8), so the trigger still does not fire — but it is now second, and 36.3
of its 55.0 ms is submits. Worth re-pricing when G11 lands, not before.

### G5's numbers, refreshed

The indexer measured **1.958 µs per context token per call** (the record's
earlier fit said 2.09), 11 calls/token → 0.0215 ms per context token:

| ctx | index ms/token |
|---:|---:|
| 2k | 44 |
| 8k | 176 |
| 32k | 706 |

At today's mean ctx of 106 it is **2.3 ms/token, 1.1% of the token** —
still nothing at short context. It overtakes the *entire* CPU-expert
bucket at **ctx ≈ 3.5k**. G5 remains exactly what §G8 called it: a product
question about deployed context length, not a short-context item.

**Correction to §G8's crossover, and a coincidence worth separating.**
§G8 closed with "after G8 the indexer is MLA's dominant term above roughly
**3.5k** context". That number used an *idealised 8×* speedup of the
attention core (650 → ~81 ms/token), not the **3.08× G8 actually
measured**. Redone with G8's own result: the core lands at 649/3.08 =
**211 ms/token** above the 2051 cap, so the indexer overtakes *it* at
**ctx ≈ 9.8k**, not 3.5k. The two numbers are easy to confuse because a
genuine ~3.5k does exist — but it is a *different* comparison, the one
above: indexer vs the whole CPU-expert bucket (74.8 / 0.0215 = 3473).
Against MLA's own attention core, which is what §G8 was ranking, G5's
trigger sits near **10k**. **This moves G5 further out, not nearer**, and
it is the second roadmap number RP1 had to correct.

### Verdict on the order

**G11 stays next**, and this profile strengthens rather than weakens that:
the CPU experts are still the largest bucket and their *share grew*,
30.8% → **37.5%**, because everything around them got faster. But two
things in its roadmap row are now wrong and are corrected there: the
expected saving (sized against 115.1, must be resized against 74.8) and
the implicit assumption that it is purely an inner-kernel ALU problem
(43% of its window's core-time is not in the kernel at all).

**Caveats.** The 23.5 ms/token histogram effect means this profile is not
a clean like-for-like with G3 on the MoE bucket specifically; every other
bucket is unaffected by it. Mean ctx 106 vs 87 makes MLA and the indexer
read slightly high here relative to G3. `perf` cannot attribute barrier
spin per region; the wait-policy table is what carries that claim. The
`perf` runs cost ~2% wall each (the timers run, without perf, is the
authoritative split, as in §G3).

## RP1-CORRECTION: RP1's CPU-expert bucket was a cold-cache artefact, and the ranking inverts

Found while starting G11, by the ordinary act of measuring the same thing
twice. **RP1's profile is right everywhere except one bucket, and that one
bucket was the whole basis for putting G11 next.**

Re-running RP1's exact configuration (fresh process, tier 1296/1695/1695,
8 threads pinned, `--greedy 128`, same frozen histogram) on a **quiet box**
with the model **100% page-resident** reproduces every bucket RP1 measured
— except the CPU experts:

| bucket | RP1 | corrected | agreement |
|---|---:|---:|---|
| `lm_head` | 2.80 | 2.80 | exact |
| `hc+norm` | 8.93 | 8.80 | 1.5% |
| router | 5.84 | 5.73 | 1.9% |
| `ffn_dense` | 2.94 | 2.89 | 1.7% |
| MLA | 24.32 | 23.97 | 1.4% |
| KDA | 54.98 | 53.33 | 3.0% |
| shared expert | 14.99 | 14.33 | 4.4% |
| **CPU int4 experts** | **74.79** | **40.26** | **1.86× off** |
| **total** | **199.4** | **162.1** | **1.23× off** |

Identical routing in both (`experts=34099`, `cpu n=8909`, `mmap serve=3358`,
`binds gpu=1056 cpu=2302`) — the same work, the same code path, 1.86× apart
on one bucket. The error localises exactly where the mechanism predicts:
**the CPU expert path is the only thing that streams ~985 MB/token out of
mmap'd model pages**, so an incompletely warm page cache taxes it and
nothing else. RP1 checked `fincore` on the *tail* shards only and read
"resident"; the full set was **93.2%** resident, and the missing 7% is
~69 MB of faults per token landing entirely on this bucket. CLAUDE.md says
it in one line — *"A cold run pays 100k+ major faults and is not a
baseline"* — and RP1 half-checked it. Corrected procedure: `fincore` across
**all 62 shards** and assert 100%, not a spot check.

Two other confounds found and removed at the same time, neither of which
turned out to matter much on its own: the box was running **netdata**
(8% of a core, and driving `dockerd` to poll constantly) — stopped for these
runs; and `ps`'s `%CPU` for `dockerd` reads 22% but that is a **4-day
lifetime average**, instantaneously it is 0.0%. The box now measures 98–99%
idle. Clocks hold 3.45–3.63 GHz throughout, so frequency is not a variable.

### What this changes: KDA is now the largest bucket, not the CPU experts

| bucket | ms/token | % |
|---|---:|---:|
| **KDA (34 layers)** | **53.33** | **32.9** |
| CPU int4 experts (G11's target) | 40.26 | 24.8 |
| MLA (11 layers) | 23.97 | 14.8 |
| shared expert | 14.33 | 8.8 |
| mHC + RMSNorm | 8.80 | 5.4 |
| router | 5.73 | 3.5 |
| dense FFN | 2.89 | 1.8 |
| lm_head | 2.80 | 1.7 |
| **total** | **162.1** | |

RP1 concluded "G11 stays next, the CPU experts are still the largest bucket
and their share *grew* to 37.5%". **That conclusion does not survive the
correction.** The CPU experts are 24.8%, and KDA at 32.9% is now the largest
remaining bucket — of which the GPU submits alone (`proj` 21.7 + `ko` 13.3)
are **35.0 ms/token, 22% of the whole token**.

**This matters beyond sizing, because it is the exact condition written into
`G4-KDA-SPEC-2026-09-04.md`:** the GPU recurrence shader is to be revisited
"after items 1–6 of the G3 list are done, **if KDA is then the largest
remaining bucket**". KDA now *is* the largest remaining bucket. The
precondition is not fully met (G11 itself is item 1 and is not done), so the
trigger has not formally fired — but the substantive half of it now holds,
and §G12 was skipped on the reasoning that it had not. **That decision is
owed a fresh look, and it is a Fable-tier call, not one to make here.**

## G11 (part 1): the expert path fused — small, bit-identical, and the item re-sized

RP1 said the expert path was worth chasing because only 57% of the
core-time in the CPU-expert window was in the kernel. Two microbenchmarks
were built to size that before touching the engine (`g11_bench.c`,
`g11_path.c`; both cycle over ~400 MB of experts so the 128 MB L3 cannot
hide the streaming, and both check every variant against the in-tree kernel
for bit-exactness before timing it).

**What the microbenchmarks said.** On the isolated kernel at 8 threads:
the in-tree kernel does 21.95 GB/s; a bit-trick nibble→float decode (which
is *exactly* bit-identical — it builds the same float by OR-ing into a
mantissa instead of `cvtepi32_ps`) gives 1.03×; adding a second independent
accumulator to break the FMA dependency chain gives **1.30×** but is not
bit-identical; four accumulators is worse than two. On the full expert
path, fusing the three `parallel for` regions into **one region with three
worksharing constructs** (gate+up share one, then the swiglu, then down) is
**bit-exact and 1.07×**; adding the faster kernel on top only reaches 1.13×.
**Once the path is fused, the faster kernel is nearly worthless** — which is
why nothing numerics-changing is shipped here.

**What the engine said.** `mlp3_cpu` now issues one parallel region instead
of three, and the swiglu is shared across the team instead of running
single-threaded between them. `GLM53_EXPERT_SPLIT=1` restores the old path
for A/B (it is not a numerics knob — both paths are bit-identical).
Four paired runs, alternating, 100% resident, quiet box:

| | split (old) | fused | Δ |
|---|---:|---:|---:|
| `[PROF] cpu` | 5.153 s | 4.945 s | **−1.62 ms/token (−4.0%)** |
| `[OPTIME] ffn_moe` | 8.938 s | 8.650 s | **−2.25 ms/token (−3.2%)** |
| `[PROF] eg` | 6.225 s | 6.021 s | −1.59 ms/token (−3.3%) |

Reproducible to 0.3% on the internal timers. **Fresh-process tok/s cannot
resolve it** — the four runs scatter 5.70–6.27 tok/s (±10%) on GPU-submit
jitter while the CPU-side timers hold to 0.3%, so for a CPU-path change the
timers are the sensitive instrument and the wall-clock is not. Stated
plainly: this is **~1% of the decode token**, not the "−25 to −45" the
roadmap carried.

**Oracle**, vs HEAD, 3 GPUs, same frozen histogram, teacher_forcing and
last_logits by `diff`, no tolerance: short prompt **bit-identical**
(`max_abs=0`), and the old path under `GLM53_EXPERT_SPLIT=1` is also
bit-identical, as it must be.

**Serving gate**, `rome_bench.sh`, against G10's 2.60/2.75:

| run | rotating median |
|---|---:|
| g11-run1 | 2.67 tok/s |
| g11-run2 | 2.71 tok/s |

**The gate cannot resolve this change and it is not claimed to.** G10's own
two runs spread 2.60–2.75; these two land inside that spread. A ~1% effect
is below this harness's resolution — which is the honest reading, not a
disappointment: the measured delta is the paired fresh-process one above
(−4.0% on `[PROF] cpu`, reproducible to 0.3% across four alternating runs),
and the gate's job here is to show **no regression**, which it does.

**Verdict.** Shipped, because it is free, bit-identical, and removes ~140
OpenMP regions per token in the direction RP1's spin analysis pointed. But
G11 as "the big kernel item" is over: its bucket is 40.3 ms/token, the path
half of it is worth ~2, and the kernel half is worth ~1.06× once the path is
fused and is not bit-identical. **The remaining money is in KDA's 35 ms/token
of GPU submits, not here.**

## Fable gate 2: the KDA recurrence goes to the GPU — reversed on the first gate's own terms

`G4-KDA-SPEC-2026-09-04.md` rejected a GPU recurrence shader and set the
condition for revisiting it: *after items 1–6 of the G3 list are done, if
KDA is then the largest remaining bucket*. §RP1-CORRECTION made both halves
true for the first time — G11 closed today, and KDA is the largest bucket
at 53.3 ms/token. The decision is **yes**, written up as
`G12-KDA-GPU-SPEC-2026-09-05.md`, and G12 is reopened as the item that
executes it.

**What changed the answer.** Not the ms alone. (1) `coli_vk_attn_qprep`
already records *batched matmuls → norm stage → matmul* into one command
buffer with compute barriers and has a CPU-reference self-test — so "from
scratch" now means one shader, the recurrence, not a chain. (2) The expert
tier already *is* a GPU/CPU numerics fork by residency, and the engine
tolerates it; what is new is that KDA state is recurrent, which the oracle
addresses with a 1000-step shader-only drift test and a long-horizon greedy
identity test rather than a tolerance. (3) `coli_kda_step` is 64
independent 128×128 tiles plus an elementwise conv: one workgroup per head.

**What the first gate got right and this one keeps.** No credit for idle
CPU cores (sequential dependency, nothing to overlap). Off by default
behind `COLI_KDA_GPU`. And the sizing is done from what is actually
removed, not from the bucket: one round trip (34 × 0.244 = 8.3 ms) plus
the CPU recurrence (18.6 ms) minus three latency-bound dispatches (5–10 ms)
= **−15 to −22 ms/token, 9–14 %** — the roadmap's "up to −35" counted
`proj`+`ko`'s matvec compute, which stays on the GPU either way, and is
corrected.

**One constraint the first spec never faced.** `kda_state`/`kda_window`
are exported as raw spans for segment migration (`glm53.c:3768–3771`).
GPU-resident state must not be read back per call — 136 MB/token over PCIe
is ~5 ms, a third of the win — so the CPU copies go stale while the knob is
on and are re-synced only at export and import. The spec names both sync
points and makes the migrate-then-continue test part of the oracle.

**Numeric stop conditions:** shader-only relative error ≤ 1e-5 over 1000
steps, no NaN/Inf; integrated greedy text identical on every `rome_bench`
prompt through 128 tokens and across the 1260-position long prompt,
last-logit cosine ≥ 0.99999 at 8/64/128; `[OPTIME] kda` **< 1.0 ms/call**
is go, > 1.2 is stop-and-ship-off. Opus executes; Fable is asked again
only if Stage 2 diverges, and the answer to loosening the oracle should be
no.

## G12 stage 1 + 2a: the recurrence runs on dev0, and the step is 2.25x faster

Executing `G12-KDA-GPU-SPEC-2026-09-05.md`. Two commits so far (`c084445`,
`e438067`); the full single-submit chain is not built yet.

**Stage 1 — the shader against `coli_kda_step`.** `kda_step.comp`, one
workgroup per head, thread `t` owning output element `v=t`. **The spec's own
LDS plan was wrong and was not followed:** it proposed staging the 128x128
state tile in LDS, but on RDNA3 that tile *is* the entire 64 KB budget with
nothing left for the reductions — and it buys nothing, because for a fixed
`k` the 128 threads already read 512 contiguous bytes. State streams from
VRAM, `memory[v]`/`result[v]` are per-thread registers (pass one needs no
cross-thread communication at all), and LDS holds ~4 KB.

`run_kda` drives it for N consecutive steps from the same state as the CPU.
At GLM's shape over **4000 steps — 31x what one layer sees in a 128-token
generation**:

| step | out drift | state drift |
|---:|---:|---:|
| 100 | 9.11e-07 | 1.21e-05 |
| 400 | 8.94e-07 | 1.03e-05 |
| 1600 | 1.28e-06 | 1.35e-05 |
| 4000 | 8.80e-07 | 8.64e-06 |

Max output drift 2.85e-06, never above 1e-5. **The state error does not
compound**: it saturates by step 100 and is lower at 4000 than at 100, which
is what `alpha<1` predicts. Catching compounding was the whole purpose of the
check. The spec's "state <= 1e-5" was written before the curve was known and
sits at a saturating noise floor; the criterion that matters, the output, is
3x under it.

**Stage 2a — the step swapped in the engine, deliberately not the chain.**
`COLI_KDA_GPU=1` replaces `coli_kda_step` alone. This *adds* a round trip
rather than removing one; the point was to measure the shader's real
in-engine dispatch cost before committing days to the chain. Four paired
alternating runs, 100% resident (asserted over all 62 shards), netdata
stopped:

| | tok/s | `kda` ms/call | `step` ms/call |
|---|---:|---:|---:|
| cpu_1 | 5.971 | 1.578 | 0.377 |
| gpu_1 | 6.458 | 1.193 | 0.168 |
| cpu_2 | 6.239 | 1.570 | 0.380 |
| gpu_2 | 6.842 | 1.190 | 0.168 |

**step 0.378 -> 0.168 ms/call, 2.25x, with the extra round trip included**;
the GPU figure repeats to three decimals. `kda` 1.574 -> 1.191 = **-13.0
ms/token**. Against the spec's go/no-go (`kda` < 1.0 go, > 1.2 stop), 1.19
is in the middle band — the expected place for a step swap still paying two
submits. Removing the second round trip (-0.244) and moving the head norm
(-0.05) now projects ~0.90 from measurement rather than estimate.

**The first attempt at this measurement was wrong, and it was the same
mistake RP1 made.** Run 1 (cpu) reported `[PROF] cpu=9.808s` and run 2 (gpu)
`5.441s` for identical routing — page cache warming between runs, which
inflated the apparent win from -13 to -33 ms/token. I had skipped the
residency assertion this record's own hardened procedure demands. Re-warmed
and alternated for the table above.

**Numerics.** `teacher_forcing` identical at every length tested and greedy
text identical through 128 decode tokens:

| prefill positions | logit cosine | tf mismatches |
|---:|---:|---:|
| 42 | 1.0000000000 | 0 |
| 210 | 0.9999904875 | 0 |
| 630 | 0.9999883082 | 0 |
| 1260 | 0.9999220376 | 0 |

Token *selection* is unaffected everywhere. Logit *values* drift with
context: at 1260 positions the cosine is 0.99992, i.e. ~1.25% relative L2.
Two honest notes on this. First, the spec asked for cosine >= 0.99999 **at
decode tokens 8/64/128**, and the CLI cannot report that — `last_logits`
prints after prefill only — so that criterion as written is not measurable;
greedy-text identity through 128 decode steps is the stronger and measurable
version, and it holds. Second, the drift is a stress-regime effect: the
harness prompts are 26-37 tokens, where the cosine is 1.0 to 0.99999. **At
1260 positions it is worse than the spec's number, and that is recorded
rather than explained away** — it is an argument against ever turning this
knob on by default without a longer-horizon greedy test, which is a decision
for the gate, not for the implementation.

### The drift is `exp`, not the reduction — and that decides deployability

The 1260-position cosine (0.99992) was below the spec's 0.99999, so before
building the chain the obvious suspect was tested: the shader tree-reduces the
q/k norm sums where `coli_kda_step` accumulates sequentially from a `norm_eps`
seed. Replicating the CPU's order exactly — one thread, 256 serial adds — is
nearly free against the 32,768 FMAs in the two passes, so if the reduction were
the source this would be a clean fix.

**It is not the source.**

| norm reduction | cosine @1260 | max_abs | tf mismatches | step ms/call |
|---|---:|---:|---:|---:|
| tree | 0.9999220376 | 0.1533 | 0 | 0.168 |
| sequential, CPU order | 0.9999217759 | 0.1505 | 0 | 0.174 |

Matching the CPU's accumulation order exactly moved the cosine by 2.6e-7 —
nothing — while costing 3.6% on the step. Reverted to the tree; the shader's
comments now record the measurement instead of the guess.

That leaves **GLSL `exp()` vs libm `expf()`** as the dominant term: ~512 `exp`
evaluations per head per step (384 in the conv SiLU, 128 in the decay), ~1.1M
per token. Removing it means reimplementing `expf`'s range reduction and
polynomial in GLSL — slower, and a second numerics path inside the numerics
path. **So the divergence is irreducible at acceptable cost, and this knob is
permanently off-by-default.** That is a conclusion about the item, not just
this commit: the value of G12 is an opt-in throughput mode, not a faster GLM.

What survives unchanged: token *selection* is unaffected at every length
tested — 0 `teacher_forcing` mismatches at 42/210/630/1260 positions, greedy
text identical through 128 decode tokens — so for greedy serving at the
harness's prompt lengths the knob is behaviourally invisible. What does not:
`max_abs` on the last logits is 0.15 at 1260 positions, which is not small,
and anyone considering defaulting this on would need a far longer-horizon
generation test than exists here.

### Stage 2b: the chain — gate met at 0.93 ms/call

`COLI_KDA_GPU=2` records a layer's eight projections, the gating, the
recurrence, the head norm and `ko` into ONE command buffer. Paired alternating
runs, 100% resident, netdata stopped:

| | tok/s | `kda` ms/call |
|---|---:|---:|
| cpu run1 | 6.552 | 1.392 |
| **chain run1** | **7.279** | **0.932** |
| cpu run2 | 6.260 | 1.570 |
| **chain run2** | **7.270** | **0.927** |

**Gate met**: the spec's go/no-go was `[OPTIME] kda` **< 1.0 ms/call** (> 1.2
stop). 0.93, repeating to three decimals. 1.481 → 0.930 mean = **−18.7
ms/token**; against stage 2a's better-warmed 1.574 baseline, **−21.9** — inside
the spec's own −15 to −22 prediction. Fresh-process +13.5%.

Numerics unchanged by the chain: greedy text identical through 128 tokens, and
the long prompt gives **0 `teacher_forcing` mismatches across 1260 positions**,
argmax same, cosine 0.9999217 — the same figure as the step-only path, so the
chain adds no drift of its own. The `exp()` floor from the previous section is
still the whole of it.

**Serving gate (after the stage 2c fix), paired alternating, 100% resident:**

| | rotating median | warm-identical | cold |
|---|---:|---:|---:|
| chain run1 | **2.92** | 3.88 | 1.50 |
| knob-off ctl1 | 2.60 | 3.49 | 1.42 |
| chain run2 | **2.88** | 3.90 | 1.59 |
| knob-off ctl2 | 2.59 | 3.51 | 1.51 |

**+11.7%** on the rotating median (2.90 vs 2.595) and **+11.1%** on
warm-identical (3.89 vs 3.50). Two columns computed from different workloads
agreeing to within 0.6 points is the reason to believe it; on the pre-fix rows
they disagreed in *sign*, which is what exposed the bug. This is the first item
on the GLM track where the serving gain (+11.7%) lands *below* the fresh-process
figure (+13.5%) rather than above it — G9 and G10 both ran the other way. No
explanation offered here; one pair of runs is not a trend, and it is recorded
so that a future item that reproduces it has a precedent to point at.

**The chain was wrong on first run, and how it was found matters more than that
it was.** Output was garbage from the first token. Rather than re-reading the
Vulkan, a non-destructive probe replayed the chain's own projection recording
and compared against `vk_batch_mv_chain`: **exactly 0** on all four outputs.
Extending the probe through the gating stage: 1e-7 on beta, 9.5e-7 on decay —
ULP-level. That left the head norm and `ko`, split by a `gpu==3` mode that
chains through the norm and runs `ko` on the CPU. Both then matched, which
meant the fault was a *parameter*, not a stage: **the head norm was handed the
recurrence's hardcoded `1e-6` as its epsilon instead of the model's `c->eps`.**
One wrong scalar in a new shader's push constants, found in two measurements.
`coli_vk_kda_layer` now takes `head_eps` separately, because conflating them is
what caused it, and the `gpu==3` mode is kept so the split is one run away.

### Stage 2c: the serving gate found a bug that every oracle had missed

The first attempt at the gate produced rows that looked like a large win and
were not measurable at all. What gave it away was not the headline:

| config | rotating median | warm-identical |
|---|---:|---:|
| g11-run2 (chain off) | 2.71 | 3.59 |
| g12 knob-off control | 2.60 | 3.51 |
| g12 chain run1 | 3.18 | **2.64** |
| g12 chain run2 | 3.16 | **2.63** |

**warm-identical is a repeated-prompt upper bound and had been above the
rotating median in every row this record has ever held.** With the chain on it
inverted. A number moving the wrong way by 35% is a fact about the code, not
noise, and the rotating median on the same rows cannot be trusted while it is
unexplained.

It was the recurrence state leaking across requests. `glm53_session_open`
callocs a fresh zeroed `kda_state`/`kda_window` per session (`glm53.c:2739`)
and hands them to `coli_vk_kda_init` (`:2753`), which opened with

    if (G.kda[layer].state) return 1;   /* already resident */

— correct for the conv taps, `alog`, `dt` and `onorm`, which are model weights,
and wrong for the state and the conv window, which belong to the *conversation*.
On a warm engine request N+1 therefore continued request N's recurrence, on all
34 KDA layers.

**Why no oracle caught it: every one of them used one request per process.**
The greedy-text diff, the 1260-position `teacher_forcing` run, the logit cosine,
the drift-saturation study, the `gpu==3` bisection — all single-request. The
bug is invisible by construction to a fresh-process oracle and can only appear
where requests share an engine, which is exactly and only the serving harness.

The control, run against a binary with the early return restored:

| request (identical prompt, greedy, one process) | fixed | pre-fix |
|---|---|---|
| 1 | reference | identical to reference |
| 2 | identical | **diverges** |
| 3 | identical | **diverges, and from request 2 as well** |

The pre-fix outputs are the failure mode worth remembering: not garbage, but a
different fluent, on-topic, entirely plausible answer each time. Nothing in the
text says "corrupted". Had the inversion been waved off as harness noise, the
chain would have shipped silently wrong for every second and subsequent request
of a serving session — the exact regime the knob exists for.

The fix re-seeds state and window from the caller's zeroed copies when the
buffers already exist, leaving the weights alone, and refuses the reuse if the
shapes disagree. `tools/rome_bench.sh`'s warm-identical column is hereby a
**correctness signal for any session-state change**, not just a performance
upper bound; a chain-off/chain-on pair that inverts it is a bug report.

**Standing rule this adds:** an oracle that runs one request per process cannot
see session-lifetime state. Any change that puts per-conversation state
somewhere new — device memory, an arena, a cache — needs a two-request identity
check (`/tmp/tworeq.py` on the rig is the pattern) before its numbers mean
anything. The stage 2b serving gate above was measured after the fix; the pre-fix rows
are kept in the datapoint log, marked INVALID, because a discarded measurement
that explains a bug is worth more than a deleted one.

## RP2: re-profile after G11 and G12 — two profiles, because the knob is off by default

The re-profile the cadence scheduled after G12. Same method as §RP1: fresh
process, tier 1296/1695/1695, 8 threads pinned, `COLI_TIMERS=1`, `--greedy 128`,
RP1's own 42-token prompt (`~/bench/prompt_glm.txt`, mean ctx 106), one frozen
histogram copy for the whole campaign, netdata stopped.

**Measured twice, because `COLI_KDA_GPU=2` ships off.** The knob-off profile is
what the engine does today; the knob-on profile is what the knob buys. They
rank the remaining work differently and both are real. Three runs each,
alternating, order counterbalanced.

| bucket | RP1-CORR | **RP2 knob-OFF** | **RP2 knob-ON** | off % | on % |
|---|---:|---:|---:|---:|---:|
| **MoE FFN total** | 105.4→ | **67.84** | **67.66** | 42.6 | 49.3 |
| — CPU int4 experts | 40.26 | 38.27 | 38.28 | 24.1 | **27.9** |
| — router | 5.73 | 5.71 | 5.76 | 3.6 | 4.2 |
| — shared expert | 14.33 | 14.27 | 14.21 | 9.0 | 10.4 |
| — GPU expert groups | — | 8.45 | 8.41 | 5.3 | 6.1 |
| — bind + dispatch + accumulate | — | 1.15 | 0.98 | 0.7 | 0.7 |
| **KDA (34 layers)** | 53.33 | **53.35** | **31.62** | **33.5** | 23.0 |
| **MLA / DSA (11 layers)** | 23.97 | 23.67 | 23.85 | 14.9 | 17.4 |
| **mHC + RMSNorm (90 sites)** | 8.80 | 9.03 | 8.89 | 5.7 | 6.5 |
| dense FFN | 2.89 | 2.90 | 2.86 | 1.8 | 2.1 |
| lm_head | 2.80 | 2.61 | 2.59 | 1.6 | 1.9 |
| **total** | **162.1** | **159.08** | **137.19** | | |

Medians of three. Accounting closes to **−0.32 ms/token** knob-off and **−0.28**
knob-on; the MoE sub-buckets close to +0.00 and +0.02. Since G9, `[PROF] eg`
*contains* the CPU expert time it overlaps, so the exclusive GPU-group cost is
`eg − cpu`; the check above fails loudly if that is ever wrong.

**Two independent confirmations fall out of this, neither of them the point of
the exercise:**

- **G12's own claim reproduces from the outside.** 159.08 − 137.19 = **−21.9
  ms/token**, against stage 2b's measured −18.7 to −21.9. Different campaign,
  different day, different runs.
- **G11 part 1's claim reproduces too.** The CPU expert bucket is the only one
  that moved against RP1-CORRECTION: 40.26 → 38.27 = **−1.99 ms/token**, against
  the −2.25 G11 measured for itself. Every other bucket is within 3% and KDA is
  within **0.04%** (53.33 → 53.35).

**The routing is bit-identical to RP1-CORRECTION** — `experts=34099`,
`cpu n=8909`, `mmap serve=3358`, `binds gpu=1056 cpu=2302` — so RP1's 23.5
ms/token histogram confound is **absent here**: the heat ranking has converged
and two more days of bench history did not move it. Knob-on reads
`experts=34098, cpu n=8910`: **one activation of 34,099 routes differently**,
which is the chain's 0.99992 cosine showing up as a single routing flip. It is
worth 0.01 ms/token and is noted because it is the first place the G12
divergence has changed anything observable at all.

### The ranking, and it depends on the knob

| knob OFF (ships today) | ms/token | | knob ON | ms/token |
|---|---:|---|---|---:|
| **1. KDA** | **53.35** | | **1. CPU int4 experts** | **38.28** |
| 2. CPU int4 experts | 38.27 | | 2. KDA | 31.62 |
| 3. MLA / DSA | 23.67 | | 3. MLA / DSA | 23.85 |
| 4. shared expert | 14.27 | | 4. shared expert | 14.21 |
| 5. mHC + RMSNorm | 9.03 | | 5. mHC + RMSNorm | 8.89 |
| 6. GPU expert groups | 8.45 | | 6. GPU expert groups | 8.41 |
| 7. router | 5.71 | | 7. router | 5.76 |

Knob-off, KDA is still the largest bucket and G12 is the answer to it — the
work is done, it is just not switched on. Knob-on, **the CPU int4 experts lead
at 27.9%**, and that bucket has already been examined twice: G11 part 2 measured
the kernel change and declined it (1.06× once the path is fused, not
bit-identical), and the bucket is VRAM-bound rather than tier-bound — 4686
experts × 14.16 MB + 3.78 GB dense ≈ 70 GB of the 72 GB the three cards have.
**There is no cheap next move on the biggest bucket in either ranking**, which
is the most useful thing this profile says.

### What is actually next: the shared expert's three submits

The largest bucket with an *unexploited* structure is the shared expert,
**14.2 ms/token in both configurations**, 0.342 ms/call over 42 layers. It runs
through the generic `mlp3` (`glm53.c:997`): `mv(gate)`, `mv(up)`, a CPU
`swiglu_clamped`, `mv(down)` — **three separate submit+wait round trips with a
host round trip in the middle.**

The fix already exists in this file and is measured: the routed expert-group
path uses the **fused `gate_up` shader chained on-device into `down`**
(`backend_vulkan.c:70`, `:826` `coli_vk_gate_up`), which is the same shape.
The shared expert simply never got it. That makes the next item **a port of a
measured pattern inside one file, not a kernel** — Sonnet tier, and bit-identical
if it reuses the same shader the routed experts already use.

Sizing honestly: two of three round trips removed, on a bucket the record has
called round-trip-dominated since §G3. That is **−4 to −9 ms/token**, and the
range is wide because the per-submit fixed cost for *this* op has not been
measured — §G12's `VK_PROF` figures were for a different one. The item should
measure its own round trip first and set its gate from that, the way G12's spec
did.

### A cliff worth recording: the KDA state is 136 MiB against a 128 MiB L3

Knob-off, `kda.step` is **bimodal** across otherwise identical runs — 6.87 vs
12.89 and 12.93 ms/token (0.199 vs 0.378 ms/call), a **47% spread** where every
other bucket in the same runs holds to 3%. Knob-on the whole bucket has a 0.5%
spread, because the state then lives in VRAM.

The arithmetic: 34 layers × 64 heads × 128 × 128 × 4 B = **136 MiB** of
recurrence state, against this box's **128 MiB** L3 (`lscpu`) — 6.25% over. A
layer's 4 MiB is touched twice in quick succession within a token, which is
free; but to still be cached at the *next* token it must survive the other 33
layers' 132 MiB of traffic. **The inter-token reuse distance is what sits just
past the cliff**, and which side a given process lands on depends on physical
page colouring, which varies per run. THP is `madvise` here, so hugepages are
not the mechanism.

Not promoted to an item: G12 already removes it, and the obvious fix — bf16
state, 68 MiB, comfortably under L3 — is another numerics knob for a bucket
that a shipped knob already handles. Recorded because a 47% bimodal swing in a
timer looks like noise until the arithmetic explains it, and the next person to
see it should not have to re-derive this.

### Procedure: asserting residency ONCE per campaign is not enough

RP1-CORRECTION's rule was "`fincore` across all 62 shards and assert 100%".
RP2 followed it, and **that was still not enough.** Part 1 asserted 100% before
its first run; part 2, starting minutes later, found the model at **91.71%
resident with 51 of 62 shards short** and aborted before measuring anything.

Every `glm53` run does this, and it is not a fluke: measured before and after
each of six runs, residency goes **100.0000% → 91.6347%, 46 shards short,
every single time, to four decimals.** Each fresh process transiently allocates
enough anon memory (expert cache, the 136 MiB KDA state, KV) to push the kernel
into reclaim, and the ~15 GiB of model page cache it drops does not come back
when the process exits. Afterwards `free` reports 59 GiB free, so **nothing
about the machine looks wrong unless you look at the model's own pages.**

That is exactly the shape of the error RP1 made, one level up: RP1 checked the
wrong *files*, RP2 part 1 checked at the wrong *time*. Both pass a check that
feels sufficient.

**Corrected procedure, and it is now the standing one:** re-warm and assert
100% residency **before every run**, not once per campaign, and report
residency after each run so the drop stays visible. `/tmp/rp2c.sh` is the
pattern. The first three runs of RP2 part 1 are discarded for this reason —
only the six runs of part 3, each individually asserted, are in the table above.

## G13: the shared expert's gate+up fused into one GPU submit -- and a
latent numeric gap in the routed-expert kernel found in the process

RP2's next item: the shared expert is 14.2 ms/token in both knob positions,
`mlp3()` calling `mv(gate)`, `mv(up)`, a CPU `swiglu_clamped`, `mv(down)` --
three separate submit+wait round trips per token, every token, never cached.

**First attempt, and why it was wrong.** The routed-expert fused kernel
(`coli_vk_expert_group`, used via `eg_prepare_submit`) looked like a direct
port: one command buffer, gate_up fused on-device, a barrier, then down --
ONE submit instead of three. `coli_vk_expert_group`'s own comment even reads
*"Synchronous form (shared expert, harness)"*, as if written with this call
site in mind. Wiring it up failed the `--logits` oracle on the very first
run: `c/shaders/qmatmul_gate_up.comp` computes `silu(gate)*up` with **no
clamp at all** -- there is no `limit` field in its push constants. GLM-5.3's
`swiglu_limit` is **10.0**, not the disabled default, and is regularly
exceeded by real activations, so the divergence was not subtle.

**This is a pre-existing gap in already-shipped code, not a bug this item
introduced.** The routed experts have gone through this exact same unclamped
kernel since the expert-group GPU path first landed (G0 era), and no oracle
had ever caught it -- because no prior change ever put a clamped (CPU) and
an unclamped (GPU) computation of the *same* operation side by side to diff
against each other. Every existing oracle in this tree compares
pristine-vs-patched on the SAME dispatch decision (same expert goes the
same route in both binaries), so a discrepancy that has existed since before
this session started was invisible until this item tried to route a
CPU-verified operation through it. **Out of scope for a one-day Sonnet port**
-- fixing the shader would touch every routed-expert computation since G2 and
needs re-validating G4/G7/G8/G9/G10/G12's own numbers. Recorded as a finding
for whoever next touches `qmatmul_gate_up.comp` or audits routed-expert
GPU/CPU parity.

**What shipped instead.** `coli_vk_matmul_pair` -- already in production
elsewhere in this repo, `kimi_k3.c`'s `vk_expert_apply`: *"w1/w3 in one
paired submit, [activation] on CPU, w2 down"*. It runs the plain matmul
shader `mv()` already uses (no fused activation, no clamp bypass), just two
independent dispatches (gate, up) in one command buffer / one submit / one
fence wait instead of two separate ones. `swiglu_clamped` and the down
projection are byte-for-byte what `mlp3` already did. **3 submits/call -> 2.**

Bit-identical because it is the same shader with the same push constants
computing the same per-row math the two `mv()` calls it replaces already
computed -- batching two independent dispatches into one command buffer
changes nothing about what either one computes.

**Oracle:** `--logits` bit-identical on both G4's short and long prompts;
greedy text identical for 128 tokens (the one line that differs in that
run's output is the self-reported wall-clock tok/s, 4.812 vs 4.813 --
timing, not text, and not part of the oracle).

**Measured**, `COLI_TIMERS=1`, paired against a pristine binary, three times:

| | pristine | patched | Δ |
|---|---:|---:|---:|
| run 1 | 0.378 | 0.293 | −22.5% |
| run 2 | 0.372 | 0.289 | −22.3% |
| run 3 | 0.368 | 0.290 | −21.2% |

`[OPTIME] shared` ms/call, mean **0.373 → 0.291**, × 42 layers = **−3.4
ms/token**. Reproduced tightly: same-binary run-to-run variance under 1%,
smaller than the width of this table.

**Serving gate: inconclusive, same situation as G11 part 1.** −3.4 ms/token
is ~2.1% of the 159.08 ms/token knob-off baseline (§RP2) -- and paired
rotating-median samples show the *pristine* binary alone swinging 3.7%
run-to-run (2.51 / 2.42 tok/s), a wider band than the effect under test.
Reporting the `[OPTIME]` figure as authoritative follows this record's own
established ranking (§RP1, §RP2): internal timers hold to ~1% where
fresh-process tok/s scatters several percent on GPU-submit jitter, and a gate
that cannot resolve an effect should say so rather than round it to zero or
manufacture significance from noise.

## SPEC-PROBE: speculative decoding is capped on this box, and not by the drafter

Asked whether the AngelSpec family (DFlash, DFlash2, DFly, DFlare, DSpark, MTP)
suits this rig. Every one of them shares a single operation — the target
verifies a **block of K tokens in one forward** — and differs only in how many
of the K a drafter gets accepted. So the ceiling here is set by how the verify
pass scales with K, which is measurable **with no drafter at all.**

`forward_prefill` already chunks by `GLM53_PREFILL_CHUNK`, and a chunk *is* a
verify-shaped span: K tokens through all 45 layers against existing context.
The prompt is fixed, so **per-token routing is identical at every chunk size** —
any change in the expert counts is pure union effect, any change in time is pure
batching. 330 tokens, mean ctx 166, 100% resident asserted before each run,
netdata stopped, one frozen histogram.

| chunk K | CPU expert calls | CPU expert time | GPU expert calls | dispatches | ms/token | vs K=1 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | **35 252** | 18.528 s | 75 628 | 13 583 | 186.6 | 1.000 |
| 2 | **35 253** | 18.393 s | 66 923 | 6 922 | 178.7 | 0.957 |
| 4 | **35 252** | 18.280 s | 57 793 | 3 486 | 172.9 | 0.927 |
| 8 | **35 252** | 18.239 s | 47 921 | 1 764 | 170.2 | **0.912** |
| 16 | **35 252** | 18.105 s | 38 462 | 882 | 172.4 | 0.924 |

**The CPU expert count is 35 252 at every block size, to the unit.** Not
approximately — exactly, with a single off-by-one at K=2. Batching tokens
produces *no* sharing whatsoever on the path that costs the most.

**Why, and it is a selection effect rather than a property of the model:** the
experts that reach the CPU are the ones the GPU tier did not want, i.e. the
cold, rarely-routed ones. The tier has already creamed off everything that
repeats. Two adjacent tokens therefore almost never share a *cold* expert,
while they share hot ones constantly — which the same table shows from the
other side: **GPU expert calls fall 75 628 → 38 462, a 49% dedup, over exactly
the same tokens.** Hot experts batch beautifully. Cold ones do not batch at all.
The bucket that is expensive is precisely the bucket that cannot be amortised.

Everything else behaves as predicted and none of it is large enough to matter:
KDA 17.850 → 16.271 s (−8.9%, the recurrence is sequential), MLA 9.083 → 7.476
(−18%, batches best), `ffn_moe` 29.721 → 27.766 (−7%). K=16 is *worse* than K=8
(56.021 vs 55.167 s), which is the chunk-sizing tradeoff `forward_prefill`'s own
comment already describes.

### What this means arithmetically

Verify-of-K costs **0.912 × K** single tokens at best. Speculative decoding
gives a speedup of `A / (0.912 K)` for `A` accepted, so it needs **A > 91.2% of
drafted tokens merely to break even.** Against the published GLM-5.3-Flash
DFlash2 acceptance of **3.85 of 8 (48%)**, this path as it stands would run at
**0.53× — nearly twice as slow.**

The generic prefill chunker is not an optimal verify, so the honest ceiling is
the floor of what a purpose-built one could reach. Take the CPU experts as
irreducibly linear (56.1 ms/token) and assume *everything else batches
perfectly* — the remaining 127.5 ms/token paid once per block:

    K=8 best case: 8x56.1 + 127.5 = 576 ms per 8 tokens vs 8x186.6 = 1493
    ratio 0.386 -> break-even acceptance 39%; at DFlash2's 48% -> ~1.23x

So **~1.2× is the optimistic ceiling**, requiring a verify path much better than
anything in the tree, plus a drafter, plus KDA state snapshot/restore. G13 is
−4 to −9 ms/token for one day of a Sonnet port. **Not worth it, and the reason
is capacity, not algorithms.**

### The condition that would reverse this

This is a conclusion about *this machine*, not about the technique. It rests
entirely on ~21% of expert activations missing VRAM and streaming from system
RAM. **If the expert tier ever held the whole model, the CPU bucket vanishes,
the linear term goes with it, and speculative decoding becomes strongly
attractive** — which is exactly why it works on the 4×GB300 deployments the
published numbers come from. Revisit if VRAM capacity changes (a fourth card,
or Q6-style int8/int4 density work landing on the GLM tier), not before.

Recorded as the answer to "is technique X suitable", for any X in this family:
the drafter was never the binding constraint.

## RP3: re-profile after G13 — the ordering did not change, and that is the answer

The re-profile RP2's own cadence scheduled after the next landed item. Same
method, run straight off the committed `tools/hot-expert/profile_run.sh` with
only the run list changed: fresh process, tier 1296/1695/1695, 8 threads
pinned, `--greedy 128`, RP1's 42-token prompt, one frozen histogram copy,
netdata stopped, **100% residency asserted before every run** (RP2's hardened
rule), three runs per knob position, alternating, order counterbalanced the
opposite way from RP2 (RP3 starts OFF, RP2 started ON).

| bucket | RP2 off | **RP3 off** | RP2 on | **RP3 on** |
|---|---:|---:|---:|---:|
| **MoE FFN total** | 67.84 | **64.98** | 67.66 | **64.68** |
| — CPU int4 experts | 38.27 | 38.76 | 38.28 | 38.59 |
| — router | 5.71 | 5.86 | 5.76 | 5.73 |
| — **shared expert (G13)** | 14.27 | **10.84** | 14.21 | **11.02** |
| — GPU expert groups | 8.45 | 8.47 | 8.41 | 8.43 |
| — bind + dispatch + accumulate | 1.15 | 1.21 | 0.98 | 0.97 |
| **KDA (34 layers)** | 53.35 | 53.43 | 31.62 | 31.66 |
| **MLA / DSA (11 layers)** | 23.67 | 23.62 | 23.85 | 24.12 |
| **mHC + RMSNorm (90 sites)** | 9.03 | 8.84 | 8.89 | 9.00 |
| dense FFN | 2.90 | 2.89 | 2.86 | 2.86 |
| lm_head | 2.61 | 2.62 | 2.59 | 2.60 |
| **total** | **159.08** | **156.77** | **137.19** | **134.90** |

Medians of three. Accounting closes to **+0.39** ms/token knob-off and
**−0.03** knob-on. **Routing bit-identical for the third campaign running** —
`experts=34099, cpu n=8909` (off) and `34098 / 8910` (on), the same figures
RP1-CORRECTION and RP2 recorded. The heat ranking is fully converged; that
confound is gone for good unless the histogram is deliberately changed.

**Residency reproduces to four decimals across a changed binary.** Every one
of the six runs left the page cache at **91.6347%, 46 shards short** — the
identical figure RP2 measured on different code, on a different day. The
mechanism is a property of the engine's allocation footprint, not of any
particular build.

### G13 confirmed from outside its own campaign — and two thirds of it arrived

The shared-expert bucket moved **14.27 → 10.84 (−3.43)** knob-off and
**14.21 → 11.02 (−3.19)** knob-on, against G13's own paired measurement of
**−3.4 ms/token**. That is the fourth item this method has now confirmed
independently of the campaign that produced it (after G11, G12, and RP2's
own pair).

**But the totals moved −2.31 and −2.29, not −3.4.** About **1.0 ms/token of
the win did not reach the bottom line**, and most of it is one bucket:

| | RP2 range | RP3 range | overlap? |
|---|---|---|---|
| CPU int4 experts, knob-off | 38.20 – 38.35 | 38.59 – 38.91 | **no**, gap +0.24 |
| CPU int4 experts, knob-on | 38.20 – 38.41 | 38.53 – 38.77 | **no**, gap +0.12 |

The CPU expert bucket is up ~1% in *both* knob positions, and the
within-campaign ranges do not overlap. **This is flagged, not explained.**
Against it: 1% is comfortably inside the ~3% cross-campaign reproducibility
this record has established (§RP2), and within-campaign spread systematically
understates cross-campaign variance — different day, different thermal
history, different allocation history, even at asserted-100% residency. For
it: it appears in both configurations, in the same direction, in the bucket
least expected to drift, and G13 did change the per-layer GPU submit pattern
that the CPU expert path overlaps with under G9. **No mechanism is
established and none is claimed.** The honest reading is that G13 delivered
its bucket in full and the token gained about two thirds of that, with the
remainder unresolved at this method's floor. Worth re-checking at RP4 rather
than theorising now: if the same offset reappears against a *different*
subsequent change, it is real and it is not G13's.

### The ranking is unchanged — in both knob positions

| knob OFF (156.77) | ms | | knob ON (134.90) | ms |
|---|---:|---|---|---:|
| 1. KDA | 53.43 | | 1. CPU int4 experts | 38.59 |
| 2. CPU int4 experts | 38.76 | | 2. KDA | 31.66 |
| 3. MLA / DSA | 23.62 | | 3. MLA / DSA | 24.12 |
| 4. shared expert | 10.84 | | 4. shared expert | 11.02 |
| 5. mHC + RMSNorm | 8.84 | | 5. mHC + RMSNorm | 9.00 |
| 6. GPU expert groups | 8.47 | | 6. GPU expert groups | 8.43 |
| 7. router | 5.86 | | 7. router | 5.73 |

Identical order to RP2, position for position. **G13 shrank #4 without
reordering anything**, and the two leaders are the same two with the same two
reasons they are not attackable: knob-off KDA is *already solved* by G12 and
merely not switched on; knob-on the CPU experts are VRAM-bound, not
tier-bound, and were examined and declined twice (G11 part 2, §SPEC-PROBE).

**A re-profile that reports "no change" has still earned its cost** — that is
what RP1's own lesson says, and it is the first time this track has run one
and had nothing to reorder.

### What RP3 actually says: the track has run out of measurable items

MLA is #3 in both positions and is the largest bucket with an unexploited
structure. Its split at ctx 106 is `proj` 0.62, `index` 0.205, `attn` 1.36
ms/call — the attention core is ~14.9 ms/token of the 24, and G8 already took
2.43× out of it. Everything below MLA is **≤ 11 ms/token**.

That bounds every remaining item on this track. A *very good* result against
any sub-MLA bucket — 30%, the order of what G7, G10 and G13 achieved — is
worth **≈ 3 ms/token, about 2% of the token.** The serving gate's
demonstrated noise floor is 3–4% (§G13: the pristine binary alone swung 3.7%
run-to-run; §G11 part 1: 1% was inside G10's spread). **Every individually
remaining item on the GLM track is now worth less than the serving gate can
resolve.** G13 was the last one big enough to be worth a gate run at all, and
even it came back inconclusive.

This is a structural statement about where the track is, not a counsel of
despair, and it leaves three honest options:

1. **Accept `[OPTIME]`-only validation** for further small items, with the
   serving gate demoted to a regression check rather than a measure of the
   win. Defensible — the timers hold to ~1% where tok/s scatters 3–4% — but
   it means shipping changes whose end-to-end effect is asserted, not shown.
2. **Attack a blocked leader**, which for both of them means changing VRAM
   capacity, not code. §SPEC-PROBE's reversal condition points the same way.
   A fourth card, or expert-density work, would unblock the CPU expert bucket
   (28.6% knob-on) *and* make speculative decoding viable in one move.
3. **Switch tracks.** Qwen3.8 is untouched, has never had a G3-equivalent
   profile, and Q0/Q5 are a day between them.

The roadmap should say which, rather than letting the next session pick the
largest remaining number and re-derive all of this.

## G14-PROBE: the CPU expert kernel is 2x off, and RP3's "no cheap move" was wrong

**RP3 concluded the two leading buckets were both blocked and recommended a
VRAM capacity change. That recommendation was wrong, and it was wrong in the
worst possible direction: it proposed buying hardware to escape the one
problem this engine exists to solve.** Colibri's premise is that a large MoE
lives in RAM with only the hot experts in VRAM. "The CPU expert path is
VRAM-bound" is not a terminus, it is the problem statement.

**What had actually been declined, versus what RP3 claimed.** RP3 said the
bucket was "examined and declined twice". It was not. §G11 part 2 tested three
*float-domain* variants of the existing algorithm — a bit-trick nibble→float
decode, two accumulators, four accumulators — and §SPEC-PROBE declined
speculative decoding, which is not about this path at all. **No one had tried a
different algorithm.**

**And §G3 has been pointing at one since the first profile**, in its own words:
`matmul_i4_grouped` is *"ALU/decode-bound at 27% of bandwidth"*, 19 GB/s against
this box's measured 70.9 GB/s. At RP3's 38.76 ms/token for 985 MB it is ~25
GB/s — **36% of ceiling**. The bucket has never been bandwidth-bound. It is
bound by how expensively nibbles become floats: per 16 weights the inner loop
spends two `cvtepu8_epi32`, two `sub_epi32` and two `cvtepi32_ps` to feed two
FMAs.

### The untried algorithm: integer-domain accumulation

Quantise the activation to int8 **once per matmul, per group** — O(I) against
the matmul's O(I·O), 0.05% at GLM's shapes — then run the inner loop as
`maddubs_epi16` + `madd_epi16` into int32 and apply the group scale once at the
end. Every convert leaves the inner loop. Nibbles stay unsigned 0..15 because
`maddubs` wants u8×s8; the stored value is n−8 and
`sum((n-8)·q) = sum(n·q) − 8·sum(q)`, so the bias comes off with a per-group sum
of the quantised activation computed in the same pass. No saturation is
possible: |n·q| ≤ 1920 and `maddubs` sums two, so |·| ≤ 3840, inside int16.

Measured with `tools/hot-expert/i4_kernel_bench.c` (G11's harness, sized to
defeat the 128 MB L3 — 86 matrices, 406 MB, cycled), 8 threads pinned, rig idle:

| variant | gate/up (4096×2048) | down (2048×4096) | numerics vs in-tree |
|---|---|---|---|
| baseline (in-tree) | 1.00× (17.2–21.4 GB/s) | 1.00× (13.4) | — |
| A bit-trick decode | 1.01–1.40× | 1.19× | **bit-exact** |
| B +2 accumulators | 1.07–1.64× | 1.28× | relL2 1.6e−7 |
| C +4 acc, 16B load | 1.26–1.65× | 1.44× | relL2 1.6e−7 |
| **D integer-domain** | **1.69–2.31×** (32.6–39.8 GB/s) | **2.23×** (29.8) | **relL2 3.9e−3, cos 0.999993** |

D lands at **42–56% of the DRAM ceiling** where the in-tree kernel sits at
19–30%. Three runs and two shapes; the *baseline itself* varies 17.2–21.4 GB/s
run to run, which is the same DRAM-placement bimodality §RP3 recorded for the
KDA state, so the ratios are quoted as ranges rather than a single figure.

### Sizing, and the numerics bill

At 2.0× on the kernel the bucket goes **38.7 → ~19.4 ms/token**; at a
conservative 1.7× on the whole path (the `[PROF] cpu` timer covers three
matmuls plus the swiglu, already OMP-fused by G11 part 1) it is
**38.7 → ~22.8, i.e. −16 ms/token**. Against RP3's totals that is **10% of the
knob-off token and 12% of knob-on** — three to four times the serving gate's
3–4% noise floor. **That projection did not survive the engine** — see the four-mode table
below: the isolated benchmark over-predicted by ~1.8x and the only
text-preserving mode delivers 1.05x.

The bill is a real numerics change: **relL2 3.9e−3 per matmul, cosine
0.999993**. That is four orders of magnitude coarser than the float-domain
variants and it compounds over 42 layers × 8 experts. It ships behind an env
knob, off by default, with the end-to-end logit diff in the commit body, per
CLAUDE.md — exactly the G12 treatment. **Whether it can ever be defaulted on is
an open question this probe does not answer**; the greedy-identity and
1260-position `teacher_forcing` oracles decide that, and they may well say no.

`matmul_i4_grouped` is on the **qwen38 path too**, so per §G10's lesson the new
kernel is a separate function and the existing one is not touched.

### What this says about RP3's conclusion

RP3's arithmetic — everything below MLA is ≤ 11 ms/token, so nothing left is
gate-resolvable — was correct *about the buckets it looked at* and wrong about
the two it had written off. The CPU expert bucket is 38.6 and has ~16 ms/token
of headroom sitting in an algorithm nobody had tried. **The lesson is narrower
than "re-profile more often": a bucket is only "blocked" if what was declined
is the same kind of thing as what is being proposed.** RP3 inherited "declined
twice" from two entries that had declined something else.

### Built, measured end to end, and it FAILS the numerics oracle

The kernel was implemented (`coli_i4_row_i8` + `coli_i4_quant_act` in
`quant.h`, wired into `mlp3_cpu` behind `GLM53_I4_INT8`, one activation
quantisation shared by gate and up) and put through the full oracle.

**Knob OFF is bit-identical to pristine on both prompts** — the default path
did not move, which was the first thing to establish and the only part of this
that is unambiguously good.

**Knob ON fails.** The greedy text *changes* — not a rounding wobble, a
materially different continuation ("Include SQL examples and discuss
trade-offs" → "Include the trade-offs of each strategy"):

| | short prompt | long prompt (1232 positions) |
|---|---|---|
| `teacher_forcing` cos | 1.000000000 | **0.995006** |
| `last_logits` cos | 0.997668 | **0.989639** |
| `last_logits` relL2 | 0.070 | **0.144** |
| argmax | same | same |
| **greedy text, 128 tokens** | **DIFFERS** | — |

Compare G12, which shipped opt-in at cosine 0.99992 with **identical** greedy
text. This is two orders of magnitude coarser and it changes what the model
says. It is not shippable as a default and it is a poor bargain even as a knob.

**In-engine speed is also below what the microbenchmark promised.** Paired,
`[PROF] cpu` over 128 tokens: **11.66 → 8.52 s**. But the two runs routed
differently (n=19706 vs 19055) *because the output diverged*, so the honest
figure is per-expert: **0.592 → 0.447 ms, 1.32×** — against 1.7–2.3× isolated.
The gap is the swiglu and the new quantisation pass, which the bench excluded,
plus real mmap streaming rather than a cycling array. At 1.32× the bucket
would go 38.6 → ~29, **−9 ms/token, not the −16 this probe projected.**

### All four variants, measured in the engine — and the real constraint

Four modes were built and each put through the full oracle against a pristine
binary. `GLM53_I4_INT8` selects: 1 = int8 everywhere, 2 = int16 everywhere,
3 = int8 gate/up with the down projection left on floats, 4 = float domain with
bit-trick decode and four accumulators. **Knob OFF is bit-identical to pristine
on both prompts in every build.**

| mode | inner loop | isolated | **in engine** | long TF | greedy text | verdict |
|---|---|---:|---:|---|---|---|
| 1 int8 | `maddubs` | 1.7–2.3× | **1.32×** | cos 0.995 | **differs** | rejected |
| 2 int16 | `madd_epi16` | — | **1.075×** | **exact** | **differs** | rejected |
| 3 int8 g/u, float down | mixed | — | — | cos 0.998 | **differs** | rejected |
| 4 float, bit-trick + 4 acc | `fmadd` | 1.26–1.65× | **1.050×** | **exact** | **IDENTICAL** | shippable, marginal |

Mode 4's timing pair is the only properly controlled one: `[PROF] cpu`
**11.777 → 11.215 s with n identical at 19706**, because the text is identical
so the routing is identical. The int8 and int16 pairs routed differently
(19706 vs 19055 / 18546) *because their output diverged*, so their ratios are
per-expert and weaker.

Mode 4's numerics are reassociation-only: short-prompt `last_logits` relL2
**3.0e−6** (cos 1.000000000), long-prompt `teacher_forcing` **exact over 1232
positions**, greedy text identical over 128 tokens. Its long-prompt
`last_logits` relL2 of 0.055 is real, not noise — **a pristine-vs-pristine
control on the same prompt returns exactly 0** — and is simply float
reassociation accumulating through 1232 positions of KV cache. Every prediction
still matches.

**§G11 was right and this probe's projection was wrong.** G11 declined this
kernel at "~1.06× once the path is fused"; measured in the engine it is 1.050×.
The G14-PROBE estimate of −16 ms/token came from extrapolating an isolated
microbenchmark, and that extrapolation does not hold.

### The constraint is bytes, not ALU — which corrects §G3 for the in-engine case

The isolated-to-in-engine attenuation is large and consistent: **2.3× → 1.32×
for int8, 1.45× → 1.05× for the float kernel, roughly 0.55–0.65× either way.**
The reason is what the two settings actually stream. The microbenchmark cycles
406 MB with partial cache hits; the engine pulls **985 MB/token of cold expert
weights out of mmap'd pages**, a different expert almost every time.

So §G3's "ALU/decode-bound at 27% of bandwidth" is right *about the kernel in
isolation* and misleading *about the kernel in the engine*. **In situ the CPU
expert path is much closer to memory-bound than the isolated number implies**,
which is why making the arithmetic 2.3× faster buys 1.32×, and why making it
1.45× faster buys 1.05×.

**That redirects the whole line of attack, and it points somewhere squarely
in-premise.** If the bucket is bound by streaming cold expert bytes, the lever
is *fewer bytes*, not faster arithmetic:

- **int3 experts** (`fmt=5`, already implemented in `upload_tensor`,
  `scale_floats` and the fused gate_up shader) cut an expert from 13.5 to
  10.5 MiB — **22% fewer bytes streamed per CPU expert**, directly against the
  measured constraint.
- The *same* change fits **~28% more experts in the same 72 GB**, so fewer
  activations reach the CPU path at all.

Both effects push the same way, and neither needs a card. That is the item this
probe actually found, and it is a better one than the kernel it set out to
build. It is a bigger job — converter, a CPU-side int3 kernel, and numerics
validation on the majority of the compute, since int3 weights are a real
precision drop unlike mode 4's reassociation — and it should be sized with an
in-engine probe, not an isolated one.

### The methodological finding, which is the more useful half

**The microbenchmark's numerics figure was meaningless and its speed figure was
not.** It reported relL2 3.9e−3; the engine showed 0.070–0.144 on logits, ~30×
worse. The bench filled its matrices and activations with **uniform random
data**. Real GLM activations are not uniform — they have the outliers that
`swiglu_limit = 10.0` exists to clamp, and one outlier in a group of 64 crushes
the resolution of the other 63.

So: a microbenchmark can size *throughput* on synthetic data, because
throughput depends on instruction mix and memory traffic, which synthetic data
reproduces faithfully. **It cannot size *accuracy* on synthetic data, because
accuracy depends on the distribution, which synthetic data destroys.** §SPEC-
PROBE's cheap-probe-first pattern is still right; this is a limit on which
questions a probe can answer, and it should have been obvious before spending
the run.

### Where this leaves the item — not dead, and re-scoped

The direction is validated: **there is 30%+ sitting in this kernel and §G3's
decode-bound diagnosis was right.** The blocker is now specific and named — the
precision of the activation quantisation, not the algorithm.

The next attempt is **int16 activations via `madd_epi16`** instead of int8 via
`maddubs_epi16`: sign-extend the nibbles to int16, keep the activation at
int16, accumulate int32. Still no float convert in the inner loop, so most of
the structural win survives, but 16 lanes per register instead of 32 — expect
~1.15–1.25× rather than 1.32×, for numerics that should be near-lossless
against f32 activations at these magnitudes. A cheaper variant worth testing in
the same run: keep int8 but quantise the activation on **sub-groups of 16**
within each 64-weight group (four int32 partials, four scales), which costs
little and directly attacks the outlier problem.

**Neither can be sized on synthetic data.** The next probe measures speed in the
microbenchmark and accuracy *in the engine*, on real activations, or it does not
measure accuracy at all.

The knob ships **off by default and is not recommended at any setting** — it is
kept as the measured baseline the int16 follow-up is compared against, not as a
usable configuration. That is stated in the code.

The second in-premise lever, unexamined and recorded here so it is not lost:
**int3 experts on the GPU tier.** `fmt=5` (int3-g64, 24B per 64-group) is
already implemented in `upload_tensor`, `scale_floats` and the fused gate_up
shader. An expert slot goes 13.5 → 10.5 MiB, ~28% more experts resident in the
same 72 GB, which cuts the CPU expert count directly rather than making it
faster. Smaller and with diminishing returns (the marginal experts are the cold
ones) and a much bigger job — converter, a CPU-side int3 kernel, and numerics
validation on the majority of the compute. Second priority, but it is a second
answer that does not need a card.

## Datapoint log (tools/rome_bench.sh)

Mechanical rows appended by the harness itself, one per run. Not curated prose;
cross-reference against the narrative sections above for interpretation.

The `engine` column names the binary, not the configuration — a `qwen38-vk`
binary with `Q38_VULKAN` unset is a CPU run. The signal that the tier is up
is the engine's own stderr line (`[qwen38] Vulkan tier preloaded N of M`,
`qwen38_core.h:1054`; `[VK] preload:`, `glm53.c:1669`), **not** datapoint.py's
`| GPU |` row: that row comes from the SERVE hwinfo frame, which qwen38 does
not send even with all three devices loaded. Rows from 2026-09-04T11:08Z on
are written only after the harness asserts those lines.

| timestamp (UTC) | engine | config | cap | max-new | threads | rotating median | warm-identical | cold |
|---|---|---|---:|---:|---:|---:|---:|---:|
| 2026-09-04T07:37:35Z | qwen38-vk | final (**GPU tier OFF**, see correction) | 512 | 80 | 8 | 3.10 tok/s | 4.67 tok/s | 2.31 tok/s |
| 2026-09-04T11:08:35Z | qwen38-vk | gpu-real1 (3 GPUs, tier asserted) | 512 | 80 | 8 | 4.12 tok/s | 5.43 tok/s | 3.47 tok/s |
| 2026-09-04T11:14:06Z | qwen38-vk | gpu-real2 (3 GPUs, tier asserted) | 512 | 80 | 8 | 4.13 tok/s | 5.44 tok/s | 3.47 tok/s |
| 2026-09-04T11:54:06Z | glm53 | g0-run1 (3 GPUs, tier asserted) | 512 | 64 | 8 | 1.66 tok/s | 2.06 tok/s | 1.23 tok/s |
| 2026-09-04T12:01:17Z | glm53 | g0-run2 (3 GPUs, tier asserted) | 512 | 64 | 8 | 1.65 tok/s | 2.07 tok/s | 1.25 tok/s |
| 2026-09-04T14:18:47Z | glm53 | g1-run1 (prefault ON, regression) | 512 | 64 | 8 | 1.48 tok/s | 2.08 tok/s | 0.99 tok/s |
| 2026-09-04T14:26:50Z | glm53 | g1-run2 (prefault ON, regression) | 512 | 64 | 8 | 1.48 tok/s | 2.07 tok/s | 0.98 tok/s |
| 2026-09-04T14:41:16Z | glm53 | g1-off-default (shipped config) | 512 | 64 | 8 | 1.64 tok/s | 2.07 tok/s | 1.27 tok/s |
| 2026-09-04T16:06:36Z | glm53 | g2-run1 (issue-all/take-all) | 512 | 64 | 8 | 1.71 tok/s | 2.15 tok/s | 1.25 tok/s |
| 2026-09-04T16:13:30Z | glm53 | g2-run2 (issue-all/take-all) | 512 | 64 | 8 | 1.69 tok/s | 2.15 tok/s | 1.27 tok/s |
| 2026-09-04T17:30:43Z | glm53 | g1b-default (prefault off, telemetry on) | 512 | 64 | 8 | 1.71 tok/s | 2.14 tok/s | 1.23 tok/s |
| 2026-09-04T18:30:00Z | glm53 | g3-timers-on (COLI_TIMERS=1 as the harness exports: instrumentation is free) | 512 | 64 | 8 | 1.70 tok/s | 2.15 tok/s | 1.24 tok/s |
| 2026-09-04T21:19:58Z | glm53 | g4-run1 (KDA parallelised) | 512 | 64 | 8 | 1.84 tok/s | 2.36 tok/s | 1.36 tok/s |
| 2026-09-04T21:26:30Z | glm53 | g4-run2 (KDA parallelised) | 512 | 64 | 8 | 1.83 tok/s | 2.35 tok/s | 1.36 tok/s |
| 2026-09-05T05:52:09Z | glm53 | g7-run1 (router parallelised) | 512 | 64 | 8 | 2.01 tok/s | 2.59 tok/s | 1.44 tok/s |
| 2026-09-05T05:58:17Z | glm53 | g7-run2 (router parallelised) | 512 | 64 | 8 | 2.05 tok/s | 2.67 tok/s | 1.47 tok/s |
| 2026-09-05T08:28:00Z | glm53 | g8-run1 (MLA heads parallelised) | 512 | 64 | 8 | 2.19 tok/s | 2.91 tok/s | 1.53 tok/s |
| 2026-09-05T08:34:09Z | glm53 | g8-run2 (MLA heads parallelised) | 512 | 64 | 8 | 2.17 tok/s | 2.83 tok/s | 1.57 tok/s |
| 2026-09-05T09:33:59Z | glm53 | g9-run1 (CPU/GPU overlap; config-name arg omitted the label at launch) | 512 | 64 | 8 | 2.33 tok/s | 3.04 tok/s | 1.58 tok/s |
| 2026-09-05T09:39:44Z | glm53 | g9-run2 (CPU/GPU overlap; config-name arg omitted the label at launch) | 512 | 64 | 8 | 2.35 tok/s | 3.05 tok/s | 1.59 tok/s |
| 2026-09-05T12:39:44Z | glm53 | g10-run1 (mHC pre/post parallelised) | 512 | 64 | 8 | 2.60 tok/s | 3.41 tok/s | 1.72 tok/s |
| 2026-09-05T12:45:11Z | glm53 | g10-run2 (mHC pre/post parallelised) | 512 | 64 | 8 | 2.75 tok/s | 3.49 tok/s | 1.71 tok/s |
| 2026-09-05T14:34:31Z | glm53 | g11-run1 (expert path fused; box quiet, 100% resident) | 512 | 64 | 8 | 2.67 tok/s | 3.60 tok/s | 1.46 tok/s |
| 2026-09-05T14:40:14Z | glm53 | g11-run2 (expert path fused; box quiet, 100% resident) | 512 | 64 | 8 | 2.71 tok/s | 3.59 tok/s | 1.58 tok/s |
| 2026-09-05T18:12:14Z | glm53 | g12-chain-run1 (**INVALID**, session-state bug: see stage 2c) | 512 | 64 | 8 | 3.18 tok/s | 2.64 tok/s | 1.58 tok/s |
| 2026-09-05T18:18:05Z | glm53 | g12-chain-run2 (**INVALID**, session-state bug: see stage 2c) | 512 | 64 | 8 | 3.16 tok/s | 2.63 tok/s | 1.49 tok/s |
| 2026-09-05T18:23:56Z | glm53 | g12-knoboff-ctl (valid; CPU path unaffected by the bug) | 512 | 64 | 8 | 2.60 tok/s | 3.51 tok/s | 1.44 tok/s |
| 2026-09-05T18:40:15Z | glm53 | g12fix-chain1 | 512 | 64 | 8 | 2.92 tok/s | 3.88 tok/s | 1.50 tok/s |
| 2026-09-05T18:46:07Z | glm53 | g12fix-ctl1 | 512 | 64 | 8 | 2.60 tok/s | 3.49 tok/s | 1.42 tok/s |
| 2026-09-05T18:51:43Z | glm53 | g12fix-chain2 | 512 | 64 | 8 | 2.88 tok/s | 3.90 tok/s | 1.59 tok/s |
| 2026-09-05T18:57:33Z | glm53 | g12fix-ctl2 | 512 | 64 | 8 | 2.59 tok/s | 3.51 tok/s | 1.51 tok/s |
